# tgjiema 审查总览与待修改报告（截至 2026-07-09）

<aside>
🧾

**用途**：汇总 tgjiema 历轮（R1–R32）审查结论，给出**当前仍需处理的待修改清单**与验收自检项，供上线收尾与后续迭代排期使用。基线：master HEAD 已推进至 `0f39117` → 修复提交 `8e86382`。**不承诺零缺陷**——本报告为静态审查汇总，运行时联调仍需实测兜底。

</aside>

## 一、当前上线裁定

| 维度 | 结论 |
| --- | --- |
| P0 / P1 严重项 | ✅ 全部闭环（自 R25 起清零，历轮未回退） |
| 最近一轮结论 | R28 @ `566f08b` 曾判 **GO**；R25/R26 判 **CONDITIONAL GO** |
| 剩余项性质 | 均为 🟡 低危 / 一致性 / 待联调 / 自检演练，**不阻断上线** |
| 最大短板 | 缺自动化测试兜底 + 关键链路未做真实环境联调 |

## 二、待修改清单（按优先级）

### P1 · 上线前必须闭环（联调 / 演练）

| 编号 | 事项 | 位置 / 说明 | 状态 |
| --- | --- | --- | --- |
| A-1 | 中继"已就绪"端到端联调 | R30-5 / R31-3：外部码采集→Up Bot 缓冲→flush→pending_uploads→idx 入库→通知 整条链路真实运行时状态 | 🟡 待联调 |
| A-2 | 备份→R2→恢复 全链路演练 | db_backup → R2 → db_restore，验证 12 张本地表 + CRDB 热表恢复一致；生产需开 `DB_BACKUP_ENABLED` | 🟡 未演练 |
| A-3 | 环形降级演练 | 主频道失效→升档+补空频道（同账号匹配）+ mon 复制内容到新备份频道，验证 15 组拓扑不丢件 | 🟡 未演练 |
| A-4 | R100 静默追赶 / 重启去重演练 | 重启后 `_get_existing_backup_ids` 去重 + 游标追平 shadow 后跳过 Telethon 拉取，确认无重复入库 | 🟡 未演练 |
| A-5 | 全仓 `py_compile`  • lint/type check | R32-1（[display.py](http://display.py) 漏 logger）潜伏 32 轮才被发现，须靠工具而非人眼兜底 | 🟡 建议纳入 CI |

### P2 · 低危代码 / 一致性（上线后按窗口清理）

| 编号 | 事项 | 位置 / 说明 | 状态 |
| --- | --- | --- | --- |
| B-1 | `relay_accounts.api_hash` CRDB 明文 | F28-1：`sync_relay_to_crdb`/`get_relay_accounts_from_crdb` 明文读写；与 config 路径（已加密 N19-2）不一致 | 🟡 待统一加密 |
| B-2 | R2 密钥加密不对称 | R26-M1：`conversation.py` 写入 `r2_secret_key` 已 Fernet 加密，但全仓无对称解密读取器；需决策 config 是否参与运行时 | 🟠 待决策+闭环 |
| B-3 | `active_files` 计数口径 | R25-L2：新码生成与派工入队两处 +1、仅删除处 -1，admin `/status` 可能漂移；声明已修但未独立确认 | 🟡 待独立复现 |
| B-4 | `get_and_reset_dead_jobs` 死代码 | F-L1：CRDB 版为未用遗留（实际死信走本地 cache_store）；声明已删但未独立确认 | 🟡 待独立复现 |
| B-5 | `deploy_vps_supervisor.sh` 以 root 运行 | R32-2：应改 `user=tgjiema`，与 `deploy_vps.sh`/`per_bot.sh` 非特权用户标准对齐 | ✅ 已修（8e86382，待复核沿用） |
| B-6 | supervisor 9001 面板误导提示 | R32-3：删除"默认无密码"文案或明确启用需设账密 | ✅ 已修（8e86382，待复核沿用） |

### P3 · 部署 / 验收自检

| 编号 | 事项 | 说明 |
| --- | --- | --- |
| C-1 | 生产 `RELAY_ENCRYPTION_KEY` 校验 | 必须已配置且与历史加密数据一致（PRE-10 解密失败即抛错，绝不回退明文） |
| C-2 | 部署后安全头验证 | `curl -I https://<域名>/health` 确认 HSTS / nosniff / X-Frame-Options / Referrer-Policy 实际下发 |
| C-3 | R2 加密→解密→SigV4 冒烟 | 配合 B-2，补一条端到端签名冒烟测试 |
| C-4 | 补审未逐行模块 | `relay_instance.py`/`relay_pool.py`/`relay_db.py`/`db_backup.py`/`db_restore.py`/`mon_bot.py` 主循环 建议补逐行审查 |

## 三、已闭环项（回归监控用）

- ✅ **R32-1** `display.py` 缺 `from loguru import logger` → 已于 `8e86382` 修复复核。
- ✅ **R25 遗留 7 项**（日志卫生、`$addToSet` 去重、`_generate_random_id` 改名、HSTS、R2 加密写入等）→ 已于 `c4ab8f96` 落地。
- ✅ **上轮全部 P0/P1**：热路径 print 清理、Dockerfile 多阶段非 root、TLS/Caddy 闭环、环形兜底 `_walk_ring_for_channel`+C3 回退、`active_files` 对称递减。
- ✅ **安全基线**：SQL 注入白名单+参数化、CSRF+速率限制+ReDoS 防护、中继 api_hash Fernet 加密、IDOR 防护、配额原子预扣+失败回滚。
- ✅ **并发一致性**：`dequeue_jobs` SKIP LOCKED 原子出队、fire-and-forget 任务引用持有、`message_backups` 幂等 upsert、死信重试上限 2。

## 四、诚实边界

<aside>
⚠️

本报告汇总自静态代码审查（R1–R32），**不含**运行时压测、真实 Telegram/CRDB/R2 联调与并发实测。R32-1 的教训表明：仅凭"blob 未变沿用上轮结论"的大文件仍存残余风险。上线前请务必完成第二节 P1 全部联调/演练项，并以 CI 工具兜底静态缺陷。

</aside>