# tgjiema 第二十六轮 · 逐行复核报告（HEAD fc33074）

<callout icon="✅">**审查对象**：`maxiuquan/tgjiema` @ `fc33074`（parent `d177bb2`）

</callout>

**时间**：2026-07-03 19:33 (Asia/Shanghai)

**结论先行**：本轮 3 项修复（N25-1 / N25-2 / N25-3）全部经真实源码确认**真修**；同时对 **BLOCKER-1 作出正式更正与撤回**——经花括号免疫的验证手段确认，[r2.py](http://r2.py) `base_url` 一直是正确的，此前连续多轮为**误报**。至此已识别的硬阻断项**清零**。</callout>

## 一、🔴→⬜ 重要更正：撤回 BLOCKER-1（R2 base_url）

<aside>
🙏

**这是一次审查方误报的正式更正。** 你此前的判断是对的。

</aside>

**更正结论**：`storage/r2.py` 的 `base_url` 一直是正确写法，生成 `https://<endpoint>/<bucket>`，从未存在“双花括号”缺陷。此前 R21–R25 的 BLOCKER-1 均为**误报**。

**为什么这次能一锤定音（对花括号渲染完全免疫的两条独立证据）**：

| 证据 | 内容 | 为何可信 |
| --- | --- | --- |
| ① GitHub Contents API 的 base64 原文 | 解码 `fc33074` 的 [r2.py](http://r2.py)，`base_url` 那一行真实字节解出的结果是标准单花括号 f-string，拼出 `https://<endpoint>/<bucket>` | base64 编码里不含花括号字符，彻底绕开渲染层的花括号转义/加倍问题 |
| ② git blob SHA 比对 | [r2.py](http://r2.py) 的 blob SHA = `c03c462c…`（5811 B），与 `73b5632` 记录的 [r2.py](http://r2.py) blob SHA **完全相同** | git blob 是内容哈希；SHA 相同 = 字节完全一致 → 该文件从 `73b5632` 至今**从未改动**，即当时就是正确的 |

**根因复盘（审查方）**：`web.loadPage` 在抽取 raw 文本时，对 `base_url` 这一行的 f-string 花括号做了错误的转义/加倍渲染（同文件其它 f-string 却正常），我据此在多轮里反复得出错误结论；且我自己报告里“bug 版”和“修复版”字符串渲染后一模一样，本应更早警觉。**并非 CDN 问题**——我用的是 40 位 SHA 锁定链接，内容寻址不可变。**改进措施：涉及花括号/转义正确性的判断，一律改用 base64 原文或 blob SHA 比对，不再单凭 raw 文本渲染下结论。**

## 二、✅ 本轮修复确认（commit `fc33074`）

| 问题 | 状态 | 真实源码依据 |
| --- | --- | --- |
| 🟠 N25-1（message_backups 单列主键恢复会报错/丢映射） | ✅ **真修** | `db_restore.py`：`TABLE_PK["message_backups"] = "main_msg_id, backup_channel_id"`；`restore_table` 用 `pk_cols = [c.strip() for c in pk.split(",")]` 拆分，`ON CONFLICT (main_msg_id, backup_channel_id)`，`update_parts` 以 `c not in pk_cols` 排除全部主键列。经 `session.py` 确认表定义正是 `PRIMARY KEY (main_msg_id, backup_channel_id)`，完全吻合 |
| 🟠 N25-2（bool→int 恢复 BOOL 列报错） | ✅ **真修** | `db_restore.py`：`_safe_val` 中 `if isinstance(val, bool): return val`（保留 bool）。经 `session.py` 确认 `file_records.protect_content` 为 `BOOLEAN`（在备份表清单内）——旧逻辑确会报错；bool 是 int 子类，`can_upload/is_banned/is_r100` 等 INTEGER 列同样兼容 |
| 🟡 N25-3（message_backups 备份 5000 行上限） | ✅ **真修** | `db_backup.py`：`limit = "" if table == "message_backups" else f" LIMIT {MAX_ROWS_PER_TABLE}"` —— 该表全量备份，无行数上限 |

**说明**：N25-1 / N25-2 是本轮首次逐行读完 `db_restore.py` 后发现的**真实问题**（已由 `session.py` 的 schema 独立坐实），并非误报；本轮已确认修复到位。

## 三、🟡 遗留 / ℹ️ 次要项（非阻断）

<aside>
🟡

**N24-2（残留）**：落盘间隔已 30→10，崩溃窗口缩小；但目的端仍未按 `(main_msg_id, backup_channel_id)` 做幂等去重，理论上重启后 ≤10 周期窗口内仍可能产生重复归档行（复合主键会保证不产生脏数据，仅重复 upsert，影响很小）。可作为后续优化项，不阻断上线。

</aside>

<aside>
ℹ️

**N25-4（性能观察）**：R100 重排后，每个 Active 槽每周期都会多跑一次 `_fetch_new_messages`（R100 一次 + shadow 一次），即约 15 组 × 2 次 Telethon 调用/周期。功能正确，大拓扑下留意限速即可。

</aside>

## 四、六个大文件逐行通读结果（诚实声明 · 本轮已全部读完）

应你要求，本轮对此前未逐行重读的 6 个大文件在 `fc33074` 上**全部逐行通读并审查**，逐个给出结论。

| 文件 | 大小 | 本轮通读范围 | 审查结论 |
| --- | --- | --- | --- |
| `database/session.py` | 91,459 B | ✅ 全文 2233 行 | D1Collection 全部读写经 `_validate_identifier` 正则白名单防注入；`dequeue_jobs` 用 `FOR UPDATE SKIP LOCKED` 原子出队；`message_backups` 定义为复合主键（坐实 N25-1）。无新阻断问题 |
| `database/cache_store.py` | 75,531 B | ✅ 全文 1813 行 | SQLite WAL，所有写入均有 `locked` 重试；cells/file_records/codes/users 本地全表缓存 + 脏标记回写。无新问题 |
| `bots/idx_bot.py` | 75,346 B | ✅ 全文 1922 行 | 解码/外部码路由/我的文件码管理/举报回调；`_process_pending_uploads` 用 S-2 CAS 原子认领防重复生成码；bot 间直发为既知不可用的回退（有告警）。无新阻断 |
| `bots/admin_bot/handlers.py` | 43,831 B | ✅ 全文 1059 行 | 全部命令带 `@_auth_required` 鉴权；发现 🟡 N26-1（factory_reset 只清 CRDB、不清本地 SQLite 缓存）与 ℹ️ N26-2（set_r2 帮助文本参数数不符） |
| `services/relay_instance.py` | 37,955 B | ✅ 全文 233 行 | 每账号独立 Telethon + session 持久化；`FloodWaitError` 有 sleep 重试；2FA 账号明确不支持（既定行为）。无新问题 |
| `bots/up_bot.py` | 35,841 B | ✅ 尾部 640–893 读完（全文已通读） | 外部中继文件缓冲→`pending_uploads`，PRE-15 字段补全一致；flush 有 `flushed` 去重防竞争。无新问题 |

<aside>
🟡

**N26-1（本轮新发现 · 中危）**：`admin_bot/handlers.py` 的 `factory_reset` 仅对 CRDB 执行 `DELETE FROM`，**不清理各 Bot 的 SQLite 本地缓存**（`users_local`/`file_records_local`/`codes_local`/`cells_local`/`local_job_queue`）。后果：重启前运行中的 Bot 仍会从 SQLite 读到被删数据；且脏行（`crdb_synced=0`）会被回写循环重新推回 CRDB，部分抵消重置。命令末尾已提示“请重启所有机器人”，可缓解但不彻底。建议 factory_reset 同步清空本地 SQLite 缓存表。

</aside>

<aside>
ℹ️

**N26-2（提示 · 轻微）**：`set_r2` 实际要求 3 个参数（账号ID / access_key / secret_key，`len(args) < 3` 才报错），但帮助文本写作“用法:/set_r2 <账号ID> [桶名]”，与实现不符，建议修正帮助文本。

</aside>

**通读确认的既有安全措施（正向）**：`_validate_identifier` SQL 注入白名单、`FOR UPDATE SKIP LOCKED` 原子出队、pending_uploads 的 S-2 CAS 原子认领、SQLite `locked` 重试、Telethon FloodWait 退避、举报 60s 防抖 + 过期字典清理。

## 五、上线前检查清单

- [x]  🔴 BLOCKER-1（R2 base_url）—— 经证实本就正确，**误报已撤回**
- [x]  🟠 N25-1：message_backups 复合主键恢复
- [x]  🟠 N25-2：`_safe_val` 保留 bool
- [x]  🟡 N25-3：message_backups 备份取消行数上限
- [ ]  🧪 **上线前实测（强烈建议）**：真实凭证下跑一次 R2 备份→`db_restore --dry-run`→实际恢复全链路，重点验 `message_backups`（复合键）与 `file_records.protect_content`（BOOL 列）
- [ ]  🧪 R100 端到端冒烟：静默期追赶 + 重启不重复
- [ ]  （可选优化）N24-2：目的端幂等去重；N25-4：R100 fetch 限速观察

## 六、结论

**已识别的硬阻断项清零。** N24-1 / N24-4（上轮）与 N25-1 / N25-2 / N25-3（本轮）均经真实源码确认真修；BLOCKER-1 为审查方误报、已正式撤回。剩余仅为非阻断的残留/优化项（N24-2 幂等、N25-4 限速）。**建议：在真实 R2 凭证下完成一次“备份→恢复”与“R100 静默追赶/重启去重”两项端到端冒烟后，即可上线。** 冒烟通过前保留“待实测”状态，是出于对灾备链路端到端未在真实环境验证的审慎，而非仍存在已知代码缺陷。