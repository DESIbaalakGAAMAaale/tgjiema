# R40 P0-3: Redis ACL 模板(由 render_acl.sh 渲染为最终 ACL 文件)
#
# 此文件是 users.acl 的规范模板,包含 <REDIS_*_PASSWORD> 占位符。
# 渲染流程:
#   1. docker-compose 启动 redis-acl-init 一次性服务,执行 config/redis/render_acl.sh
#   2. render_acl.sh 从 REDIS_*_PASSWORD 环境变量读取密码(缺失时 openssl rand 随机生成)
#   3. sed 替换占位符 → /data/users.acl(校验无 '<' / 'changeme' 后 chmod 600 + chown redis:redis)
#   4. redis 主容器以 --aclfile /data/users.acl 加载最终 ACL
#
# 兼容性:
#   - 裸机部署(systemd)由 deploy_vps_per_bot.sh 调用 render_acl.sh 或直接 sed 替换
#   - 旧版 deploy_vps_per_bot.sh 仍读取 config/redis/users.acl(保留作为兼容备份)
#
# 修复历史:
#   R39 P0-1: 新增专用 health 用户(仅 PING),修复 docker-compose healthcheck 认证失败
#   R39 P0-2: 占位符密码 + 正向白名单 + key 限制到 tgjiema:* 命名空间
#   R40 P0-3: render_acl.sh 容器化渲染,首次 `docker compose up` 不再加载原始占位符 ACL
#
# 注意: ACL 规则按从左到右顺序评估,后出现的规则覆盖先出现的。
#       因此正确写法是 `-@all +cmd1 +cmd2`(先拒绝全部,再逐个放行),
#       而非 `+cmd1 +cmd2 -@all`(后者会让 -@all 覆盖前面的 +cmd)。

# ── default 用户: 禁用(不允许任何连接) ──
user default off

# ── health 用户: 仅用于 docker-compose healthcheck 的 PING 命令 ──
# R39 P0-1: 专用 healthcheck 用户,密码由 REDIS_HEALTH_PASSWORD 注入
# 仅允许 PING 命令,不能读写任何 key(~* 仅为语法占位,实际无任何 +命令能访问 key)
user health on ><REDIS_HEALTH_PASSWORD> ~* &* -@all +PING

# ── tgjiema_writer: 写入用户(Stream 消费 + 写入 + kv_store 基本操作) ──
# R39 P0-2: 正向白名单,仅允许业务所需命令;key 限制到 tgjiema:* 命名空间
# 用途: db_writer / up_bot / idx_bot / dsp_bot / mon_bot / admin_bot / admin
#       共享此用户(写权限),由 REDIS_URL 中嵌入凭证实现单一连接点
user tgjiema_writer on ><REDIS_WRITER_PASSWORD> ~tgjiema:* &tgjiema:* -@all +XADD +XREADGROUP +XACK +XLEN +XPENDING +XCLAIM +XTRIM +XINFO +XDEL +PING +EXPIRE +TTL +SET +GET +DEL

# ── tgjiema_reader: 只读用户(消费组读取 + kv_store 只读) ──
# R39 P0-2: 正向白名单,仅允许读命令;key 限制到 tgjiema:* 命名空间
# 用途: 仅需读取的服务(如 prometheus_exporter)或调试用途
user tgjiema_reader on ><REDIS_READER_PASSWORD> ~tgjiema:* &tgjiema:* -@all +XREADGROUP +XINFO +XLEN +XPENDING +PING +GET +TTL

# ── tgjiema_admin: 管理员用户(mon_bot/admin_bot/admin 后台使用) ──
# R41 P1-9: 拆分 writer 用户为 4 个角色,管理员需要额外权限:
#   - XAUTOCLAIM: 自动认领超时未确认的 pending 消息(leader lease 切换时)
#   - EVAL: 执行 Lua 脚本(原子操作:多 key CAS / fencing token)
#   - XTRIM: 主动裁剪 Stream(超出保留窗口的旧消息)
#   - 全部 Stream + KV 命令(管理操作:重置消费组 / 修复卡住的 PEL)
#   - CONFIG GET: 读取运行时配置(用于监控/诊断,不含 SET 防止误操作)
#   - CLUSTER NODES / INFO: 集群状态查询(单机模式下也兼容)
# 用途: mon_bot / admin_bot / admin 后台服务,需要管理 Redis 状态
# 安全: 仍限制在 tgjiema:* 命名空间,不能访问其他业务的 key
user tgjiema_admin on ><REDIS_ADMIN_PASSWORD> ~tgjiema:* &tgjiema:* -@all +XADD +XREADGROUP +XACK +XLEN +XPENDING +XCLAIM +XAUTOCLAIM +XTRIM +XINFO +XDEL +PING +EXPIRE +TTL +SET +GET +DEL +EVAL +CONFIG +CLUSTER +INFO +CLIENT +TIME +MEMORY +SLOWLOG +LATENCY
