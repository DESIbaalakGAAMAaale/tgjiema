# tgjiema R66 正式上线前全仓终审与一次性整改建议（HEAD 93a2497）

<aside>
⛔

**R66 终审裁定：DEVELOPMENT PASS / STAGING CONDITIONAL GO / PRODUCTION NO-GO。** 最新四条 master 工作流虽为 success，但 Release Gates 存在可复现的“绿灯不等于生产证据”问题：已提交 migration manifest 与当前 HEAD 不一致；发布 job 在构建镜像后才重生并签名 manifest，签名对象并不在已构建镜像中；跨工作流校验把“无 run / queued / in_progress”视为 SKIP；真实 RU/恢复/soak 证据缺失时相关 job 仍可 success；tag 触发时 cosign identity 仍按 `refs/heads/<tag>` 拼接。

</aside>

## 1. 审查基线与边界

- 仓库：[maxiuquan/tgjiema](https://github.com/maxiuquan/tgjiema)
- R66 HEAD：`93a24970cdf39a81bb0f8f938d82ddbbce9c9acb`
- Tree：`1022d7fea04f44cbacb4da374627e8b89811de83`
- 代码 HEAD（自审文档提交之前）：`b4c5688faa8be252b4f3d8f4b34d852443c66dca`
- 上轮审查基线：R65 `20bf989a4b1017296d5237288290aefadce2205b`。[[1]](https://app.notion.com/p/tgjiema-R65-HEAD-20bf989-67b4c252e50e45a387994fb5e8f6cb94?pvs=21)
- 最新提交时间：2026-07-20 12:01:37（Asia/Shanghai），签名 verified/valid。
- 审查口径：以仓库源码、manifest 和 Actions API 结果为准；提交说明、自审报告和测试数量不作为问题关闭的唯一证据。
- 验证边界：本轮取得公开 GitHub API、源码与 Actions job 级证据；未持有生产 CRDB、R2、Redis、Telegram、VPS、账单和密钥，不能替代真实生产演练。

## 2. 权威 Actions 结论

最新 HEAD `93a2497` 的 Release Gates run [29715917039](https://github.com/maxiuquan/tgjiema/actions/runs/29715917039) 为 success；同 SHA 的 CI、E2E Tests、Deploy Check 也为 success。这证明当前自动化集合未检测到失败，但**不能推出 Production GO**：

- `crdb-ru-72h-attribution-gate` 在未下载到 `ru_72h_data` 时仍 success，随后“上传结果”步骤 skipped。
- `migration-manifest-gate` success，但仓库 manifest 的 `release_commit` 仍是 `36faa435...`，不是当前 `93a24970...`。
- 普通 master push 的 `production-promotion-gate` skipped，尚无成功 tag 晋级证据。
- Release Gates 的 success 因此是“代码/合约门禁通过”，不是“生产证据齐备”。

## 3. R65 整改复核矩阵

### 已真实改善

- cosign 镜像签名与 verify 已在当前 branch run 中成功；同一 run 的 sign-image、publish-attestation、release-summary 均 success。
- a11y enforcement 已改为 `--test-report` 必填；无报告、解析失败、skip、flaky、timedOut、unexpected 均失败。
- RestoreBackend、ApprovalAuthority、MFAAuthority 和 UoW/CAS 路径已加入，方向正确。
- branch protection、按钮 UX、i18n/error scanner、typed sink 等门禁数量显著增加。

### 仍未关闭或被“绿灯”掩盖

- 生产 tag 的签名 identity 拼接错误风险未关闭。
- migration trust anchor 与 Git tree 无稳定绑定模型。
- build-once 镜像与运行后重生的 manifest 不是同一内容集合。
- publish-attestation 对兄弟 workflow 未完成/不存在采取 fail-open。
- restore 仍保留无 backends、无 authorities 的兼容降级路径。
- legacy restore scanner 白名单覆盖真正 writer/strict service，且语法解析失败会跳过文件。
- 72h RU、7d soak、3 次空白恢复和真实 provider chaos 尚无不可变生产证据。

## 4. P0 生产阻断项

### P0-01 · 当前仓库 migration manifest 明确过期，门禁却为绿

**证据：**`database/migrations/migration-manifest.json` 写入：

- `release_commit = 36faa43548ff33ba13b4d8303bf4cb78f808bfc5`
- `tree_sha = c6308b163e5f3a54c6cff7df9dba7836b03ba20c`

当前 HEAD/Tree 则为 `93a24970...` / `1022d7fe...`。其后至少经过 manifest 提交、workflow 修复和自审文档提交，所以 trust anchor 已不绑定当前 release。与此同时最新 `migration-manifest-gate` 仍 success，说明 branch 级 strict gate没有执行 HEAD/Tree 等值验证。

**根因：**把“包含自身 digest 的、提交到 Git 的 manifest”同时要求绑定当前 commit/tree，形成自引用：更新 manifest 会产生新 commit/tree，立即再次过期。

**一次性整改：**

1. Git 中的 migration catalog 只保存 migration 集合、顺序、hash、DDL version、rollback strategy，不保存当前 commit/tree。
2. 在 checkout 后、**docker build 前**生成不可变 release manifest，绑定 source commit/tree 与 migration catalog digest。
3. 将该 release manifest COPY 进镜像；构建完成后再补 image digest 会产生循环，应使用 OCI attestation/referrer 单独绑定 image digest，而不是改写镜像内文件。
4. 对镜像内 catalog digest、外部 release manifest、OCI image digest、source commit/tree 做四方验证。
5. 删除“提交一次 manifest 来绑定当前 HEAD”的 PR 流程；该模型不存在稳定点。

**验收：**任意 docs-only commit 后，branch gate 能识别旧 catalog/manifest；release artifact 与镜像内 catalog digest 完全一致；无需再提交“重新绑定 HEAD”的 manifest commit。

### P0-02 · build-once 后才重生 manifest，签名对象不在已构建镜像内

release workflow 注释和步骤显示：`docker-build` 已完成后，`sign-image` 才执行 `Regenerate migration manifest`。这只能修改 runner 工作树，不能修改已经推送的 OCI image。随后签名的是 runner 上的新 manifest / release manifest，而镜像仍包含 checkout 时的旧 manifest。

**影响：**供应链证据可以声称 migration manifest 绑定当前 SHA，但实际部署镜像内是另一份文件；“同 digest 全链闭环”不成立。

**整改：**生成顺序固定为：source catalog 校验 → 生成 source-bound release metadata → Docker build once → 镜像内 catalog digest 校验 → cosign sign image digest → 生成并签名 OCI attestation。禁止 build 后修改任何声称位于镜像内的发布输入。

### P0-03 · tag 发布的 cosign certificate identity 按 branch 形式拼接

workflow 同时监听 `v*.*.*` tag，但 verify 使用：

`...release-gates.yml@refs/heads/${{ github.ref_name }}`。

对 tag event，`github.ref_name` 是 tag 名，而 Fulcio workflow identity 应绑定 `refs/tags/<tag>`，不是 `refs/heads/<tag>`。当前 branch run 成功不能证明 tag run 可成功。

**整改：**直接使用完整、受信的 `${{ github.ref }}` 构造精确 identity，并显式只允许：

- branch CI：`refs/heads/master`；
- production promotion：受保护、符合版本策略的 `refs/tags/vX.Y.Z`。

增加一次真实候选 tag 端到端演练；验证 sign image、sign blob、verify、attestation、promotion 全部通过且 identity 为 `refs/tags/...`。

### P0-04 · publish-attestation 对兄弟 workflow 状态 fail-open

最新修复把“无 run”以及 queued/in_progress 视为 SKIP，并保持 `ALL_GREEN=true`，理由是依赖 branch protection。该逻辑不适用于：

- 直接 push 后并行 workflow 尚未创建或尚未完成；
- tag 发布（branch protection 不保证 tag 的兄弟 workflow）；
- workflow API 延迟、权限错误或查询失败。

这允许 attestation 在 CI/E2E/Deploy Check 未证明成功时先发布。

**一次性整改：**

1. 改为单一 reusable workflow / workflow_call 编排，由 release job 对 CI、E2E、Deploy Check 的明确 outputs 建立 `needs` 依赖。
2. 若必须跨 workflow，使用 `workflow_run` 聚合器并轮询固定 SHA，设置超时；missing/queued/in_progress/API error 均是 pending，超时后 fail。
3. attestation 只在所有 required checks 对**同一 SHA** completed+success 时生成。
4. tag promotion 重新验证 required checks，不借用 PR branch protection 推论。

### P0-05 · 生产证据缺失时 gate 仍 success

最新 job 证据显示 `crdb-ru-72h-attribution-gate` 未取得数据 artifact 仍 success。普通 push 的 production evidence 本来只能是 dry-run，但 run 总结和自审报告容易把其纳入“全部通过”。

**整改：**

- 明确拆分 `code-release-gates` 与 `production-promotion-gates`。
- 普通 push 的缺证据 job 应输出 `not_applicable`，不得叫 production gate，也不得计入 Production GO。
- tag/environment promotion 时，RU 72h、7d soak、三次 restore、outbox chaos、supply chain 五类证据缺失、下载失败、签名无效、环境不匹配或过期，一律 failure。
- 所有证据绑定 commit、tree、image digest、environment ID、开始/结束时间、执行者/审批者和原始数据 digest。

### P0-06 · RestoreOrchestrator 仍保留生产可达的降级骨架

当前实现明确写明：

- `backends=None` 时保留“旧骨架行为”；
- `approval_authority=None` / `mfa_authority=None` 时保留旧 ID 比较路径；
- 内存 `_operations` 仍存在；
- compatibility path 只记录状态或 switch，而不保证真实 backend 与权威 capability。

“生产应提供”不是可执行安全边界。任何 wiring 漏配、测试构造被复用或环境错误，都可能回到 R65 已判定不安全的路径。

**整改：**

- 删除 production class 的 Optional 依赖；构造时缺 backend/authority/store 立即抛错。
- 将兼容骨架移动到 tests-only fake，不得位于生产模块。
- operation 每次从权威 store 重载；缓存仅作带版本快照，不得决定 phase。
- startup readiness 必须验证三个 backend、authority、nonce ledger、active pointer/fencing store 全部可用。

### P0-07 · legacy restore “seal”存在宽白名单和解析失败放行

scanner 白名单允许 `db_restore.py`、`backup_dr_validate.py`、`restore_backends.py`、`restore_orchestrator.py` 直接调用 legacy writer；恰好覆盖最危险的旧入口与适配层。脚本还在 Python 解析失败时跳过文件，不报错。

**影响：**旧原地覆盖 writer 仍可经白名单内部方法或新增 wrapper 被调用；语法/编码异常可让扫描器漏检。

**整改：**

- legacy writer 移出生产包，编译/打包时不进入镜像；或要求不可伪造 capability 参数并只由 backend 持有。
- scanner 的 parse error 必须 fail。
- 白名单从“整个文件”改为精确函数+行范围+AST 调用关系；禁止 wrapper 再导出。
- 增加 runtime test：生产镜像 import legacy writer 必须失败；`ALLOW_LEGACY_RESTORE` 在生产配置中出现即启动失败。

## 5. P1 上线前必须完成

### P1-01 · Release Gates success 的语义需降级

当前普通 push 会 build/push/sign 新镜像，即使只是新增自审文档。建议将 PR/push 分为 build-test（可不推送或推送临时 namespace）与 release candidate（显式版本/tag/environment）两条路径，避免每个文档提交产生可被误认作候选的签名镜像。

### P1-02 · migration gate 的两套校验口径不一致

branch `migration-manifest-gate` 只检查结构/hash/策略，真正 HEAD 绑定在后续 sign-image 内临时重生后验证。应统一为一种 release artifact 模型，禁止一个 gate 对仓库旧 manifest 报 success、另一个 job静默重写它。

### P1-03 · tag 路径没有同等 E2E 证据

当前成功证据均来自 branch push。必须增加 ephemeral RC tag 或 workflow_dispatch + ref 输入的演练，覆盖 tag identity、permissions、environment approval、artifact retention、promotion、rollback 和防重放。

### P1-04 · RU gate 需要区分“无数据”与“通过”

下载 action 的 optional 行为可用于普通 CI，但必须输出机器可读状态：`evidence_present=false`；release-summary 不得把它显示为 passed。promotion 则必须要求 `evidence_present=true`。

### P1-05 · restore 必须做真实多数据源一致性与故障注入

测试需覆盖 CRDB、cache SQLite、relay SQLite 的同一 backup point；在 provision/load/validate/prepare/commit/rollback 每阶段注入 kill -9、磁盘满、网络分区、CAS 冲突。验收要求 active 始终是单一时间点，失败时 staging 可回收，approval/MFA 不被错误消费。

### P1-06 · Approval/MFA/外部切换的原子性边界要明确

数据库 UoW 不能原子回滚已经完成的文件 rename 或 CRDB routing switch。应使用 prepare/commit intent + fencing token + recovery reconciler；崩溃恢复必须可根据持久化 intent 判定完成或回滚，而不是假设 `async with UoW` 能撤销外部副作用。

### P1-07 · 真实 provider 幂等与 outbox fencing 仍需生产证据

接口和单元测试不能证明 Telegram/R2/CRDB 外部副作用恰好一次。对每类 provider 建立 idempotency key/receipt ledger，执行 crash-after-send-before-ack、lease expiry、worker takeover、DLQ replay；确认不会重复发送或永久丢失。

### P1-08 · a11y 严格脚本已改善，但需保留原始报告

64/64 结果应作为不可变 artifact 保存并绑定 SHA；报告解析器需校验测试文件/项目/浏览器与矩阵 case 一一对应，避免仅伪造 stats 数字。

### P1-09 · 测试跳过项必须分类治理

自审报告称全量 `6037 passed, 142 skipped`，并排除 Telethon 依赖测试。上线前必须生成 skip inventory：原因、owner、到期日、生产影响。涉及 Telegram 主链、恢复、密钥、部署的 skip 不得归为无关历史问题。

### P1-10 · 供应链 statement 需验证语义而不只验证签名

除 cosign verify 成功外，还必须断言 statement subject digest、source repository、source commit/tree、workflow ref、builder identity、issuer、Rekor inclusion、certificate validity、SBOM/provenance predicate type 与 release manifest 完全一致。

### P1-11 · branch protection 不能作为 tag 发布的充分条件

为 tags 配置不可变、禁止删除/移动的 ruleset；生产 environment 需要独立审批、最小权限和 digest-pinned deploy。禁止用 master 的 required checks 推导 tag 已满足相同条件。

### P1-12 · 正式发布必须从“连续三次绿”改为“同候选不可变复验”

连续 rerun 同 workflow 只能证明可重复执行，不能证明候选不可变。应固定 commit+tree+image digest+attestation digest，三次验证全部消费同一候选，不得每次重新 build。

## 6. SDLC 八阶段结论

1. **需求：条件通过** — SLO/RTO/RPO/RU/安全策略已较完整，但 Production GO 语义需拆分。
2. **架构：不通过生产门槛** — release metadata 自引用、build 后改 manifest、restore 可降级。
3. **设计：条件通过** — backend/capability/fencing 方向正确，兼容旁路削弱 fail-closed。
4. **实现：不通过生产门槛** — 当前 manifest 明确过期；tag identity 与跨 workflow聚合存在硬缺陷。
5. **验证：条件通过** — branch CI/E2E 全绿；真实 tag、生产恢复、RU/soak/chaos 未证实。
6. **发布：不通过** — branch run 不能替代 tag promotion；attestation 可在兄弟 workflow 未完成时生成。
7. **运维：不通过生产门槛** — 缺真实 72h RU、7d soak、3x blank restore 和 provider chaos artifact。
8. **持续改进：条件通过** — 门禁很多，但应减少重复 scanner、白名单和“兼容模式”。

## 7. 一次性整改顺序

### Wave 0 — 修正发布真值模型

- 拆除 committed manifest 的 commit/tree 自引用。
- 修复 tag identity 为完整 `${{ github.ref }}`。
- publish-attestation 对所有 required workflow 改为同 SHA completed+success 才通过。
- 将代码 gate 与生产证据 gate 分离。

### Wave 1 — 确保构建输入与签名对象一致

- manifest/catalog 在 build 前冻结。
- build once 后禁止修改发布输入。
- OCI attestation 独立绑定 image digest、source、catalog、SBOM、provenance。

### Wave 2 — 删除恢复降级路径

- production orchestrator 强制 backends/authorities/store。
- legacy writer 不进入生产镜像；scanner parse error fail。
- 外部切换采用 intent/fencing/reconciler。

### Wave 3 — 真实环境验证

- 同一不可变候选完成 3 次 blank restore、全阶段 chaos、7d soak、72h RU、provider idempotency。
- 保存签名原始 artifact，不接受人工摘要或 dry-run。

### Wave 4 — 最终发布演练

- 创建受保护 RC tag；完整跑 tag workflow。
- 验证 environment approval、tag ruleset、digest-pinned deploy、回滚。
- 同一候选复验三次，之后才给 Production GO。

## 8. Production GO 硬门槛

- [ ]  Git 中不再存在“必须等于包含自身的 HEAD/tree”的自引用 manifest。
- [ ]  镜像内 migration catalog digest 与签名 release/OCI attestation 完全一致。
- [ ]  RC tag cosign identity 为 `refs/tags/<version>`，全链 success。
- [ ]  CI/E2E/Deploy Check 对同一 SHA completed+success 后才允许 attestation。
- [ ]  普通 push 的 missing production evidence 不再显示为通过。
- [ ]  三个 restore backend 与 approval/MFA authority 为生产强制依赖，无兼容降级。
- [ ]  legacy restore writer 不进入生产镜像，scanner 解析失败即 fail。
- [ ]  三次空白环境恢复与全阶段故障注入通过。
- [ ]  7d soak、72h RU、真实 provider chaos 证据签名且未过期。
- [ ]  142 个 skipped 已分类；关键主链 skip 为 0。
- [ ]  tag ruleset、production environment 审批、digest-pinned deploy、rollback 演练通过。
- [ ]  三次验证消费同一 commit/tree/image/attestation digest，不重新 build。

<aside>
📌

**R66 最终结论：**R65 的大量代码整改是实质进步，branch 工作流也首次整体转绿；但当前绿色 Release Gates 仍包含多个 fail-open/语义错配。最优先不是继续增加测试数量，而是修正发布真值模型：消除 manifest 自引用、确保 build 前冻结输入、tag identity 正确、跨 workflow 聚合 fail-closed、生产证据缺失即阻断，并彻底删除 restore 兼容降级。完成上述 P0、真实 tag 演练和生产证据后，才可给出 Production GO。

</aside>