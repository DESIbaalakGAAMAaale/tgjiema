# tgjiema R63 正式上线前全仓逐文件、逐功能、逐调用链终审与一次性整改报告（HEAD 5d3f665）

<aside>
⛔

**R63 最终裁定：DEVELOPMENT PASS / STAGING CONDITIONAL GO / PRODUCTION NO-GO。** 本轮提交确实关闭了 R62 的部分表面问题，但尚未达到正式上线条件。当前 SHA 的 Release Gates 仍失败；恢复载荷存在“冻结外壳、可变内核”的验证后篡改窗口；恢复过程跨 CRDB 与两个 SQLite 库非原子；migration manifest 与当前 HEAD/Tree/004 migration 不一致且其签名步骤实际跳过；高风险 outbox 仍是会在无 provider 时直接标记完成的 stub。

</aside>

## 1. 审查基线与方法

- 仓库：`maxiuquan/tgjiema`
- R63 权威 HEAD：`5d3f66549cd9833351eb1d3319e95b7adfdd0eb7`
- Tree：`b165e11e5bf63bdf832c06a5fed68f1b7bdc78c6`
- 父提交/R62：`17241a88026aaeca47aae65d917d50bb1c388ebc`
- R62→R63：ahead 1 commit；提交统计约 `+17488/-832`，签名 verified/valid。
- 审查对象：递归仓库树、R62→R63 实际差异、恢复/备份、迁移、审批/MFA、按钮、UoW/outbox/effect receipt、i18n/错误码/a11y、CI/Release、CRDB RU 和部署证据链。
- 判定原则：commit message 只作线索，不作关闭证据；以实际源码、数据库迁移和 GitHub Actions conclusion 为准。
- “全文逐行”口径：对递归树建立覆盖清单，对变更文件和高风险调用链做逐语句核查，对未变更模块按接口、门禁、依赖和运行证据回归。报告不会虚构“未来绝对无缺陷”。

## 2. 执行摘要

### 2.1 本轮真实改进

1. restore strict API 已删除公开的 `skip_strict_validation` 与 override 参数；writer 首部开始调用 capability 断言。
2. 按钮 handler baseline 从 4 降为 0；report/restore/delete 引入持久 nonce、短 handle 和 CommandBus。
3. effect receipt 改用 request hash 冲突检查与终态保护，并新增 004 migration。
4. 高风险 UoW 增加 `outbox_events`，试图将外部副作用移出 SQLite 事务。
5. MFA 增加 receipt age、吊销 ledger，并保留 `kid` 密钥轮换。
6. a11y 测试删除依赖缺失自动 skip；login en-US 使用独立 context/cookie 并断言英文渲染。
7. 错误定义已显式包含 `presentation/show_retry_button/audit_level`。
8. i18n 新增 `UserMessage` 与有限的跨函数 scanner。

### 2.2 当前上线阻断事实

- Release Gates run `29639002537` 失败。
- `rc-continuity` 失败。
- `sign-image` 中 OCI 签名成功，但紧接着的签名验证失败。
- SLSA provenance、source artifact 签名、migration manifest 签名/验证/commit 绑定检查和 signed artifacts 上传全部跳过。
- `publish-attestation` skipped，`release-summary` failure。
- 当前 migration manifest 仍绑定 R62 HEAD/Tree，且没有列出新 004 migration。
- 高风险 `OutboxWorker` 明确标注为 stub；`provider_registry=None` 时会把事件直接标记 completed，等价于静默丢弃副作用。

## 3. GitHub Actions 权威结论

| 工作流/门禁 | 运行或证据 | 结果 | 结论 |
| --- | --- | --- | --- |
| Deploy Check | 29639002515 | success | 部署静态检查通过 |
| E2E Tests | 29639002553 | success | 当前自动化用例通过 |
| Release Gates | 29639002537 | failure | 生产硬阻断 |
| rc-continuity | Verify RC continuity | failure | 连续候选证据不足 |
| sign-image | Verify image signature | failure | 签名策略仍不匹配 |
| migration manifest signing | sign/verify/binding | skipped | 迁移可信根未生成 |
| publish-attestation | job | skipped | 证明链未闭环 |
| release-summary | required jobs | failure | 禁止 Production GO |

**R63 与 R62 的关键差异**：虽然 workflow 增加了 migration manifest 签名步骤，但由于 image verification 先失败，后续所有签名、provenance、attestation 步骤仍未执行。代码存在不等于发布证据存在。

## 4. P0——正式上线前必须全部关闭

### P0-01：Release 供应链仍失败

**证据链**：keyless image signing 成功；image signature verification 失败；之后 provenance、source signature、migration manifest signature、manifest binding 和 artifact upload 跳过；attestation 未发布。

**根治逻辑**：

1. 签名与验证都只使用 `${IMAGE}@${DIGEST}`。
2. 从实际 Fulcio certificate 提取 identity，再以精确 workflow identity 或经转义的 regexp 验证；签名与验证使用同一 issuer/identity 规则。
3. 将 verification statement 作为后续 provenance/attestation 的强依赖输入。
4. 每个 required job 只接受 `success`；skipped/neutral/cancelled 一律失败。
5. deploy 仅接受 Release job 输出的 digest，不允许重新解析 tag。

**验收**：连续 3 次全新 run 全绿；同一 digest 独立验证 image signature、source signature、SBOM、SLSA provenance、migration manifest signature、attestation 与实际部署对象。

### P0-02：VerifiedBackupPayload 只是浅冻结，存在验证后篡改窗口

**证据**：`VerifiedBackupPayload` 使用 `@dataclass(frozen=True)`，但 `tables` 与 `payload` 仍为可变 `dict`；`__post_init__` 只计算一次 `payload_digest`。writer 调用 `assert_valid(verified_payload.payload_digest, ...)`，比较的是已经存储的 digest，而不是对即将写入的 `tables/payload` 重新计算。

**可利用路径**：

```python
verified = VerifiedBackupPayload(payload=data, tables=data["tables"], ...)
data["tables"]["users"][0]["role"] = "admin"
# verified.payload_digest 仍是旧值；verified.tables 已变化
await _restore_from_backup_data(verified, _capability=cap)
```

frozen dataclass 只阻止字段重新赋值，不阻止嵌套 dict/list 修改。

**根治逻辑**：

- 验证后将 payload canonical bytes 化，writer 只接收不可变 bytes；写入前重新计算 SHA-256。
- 或递归转换为 `MappingProxyType + tuple + frozen scalar DTO`，但仍需在 writer 第一行对实际序列化 bytes 重新 hash。
- `tables` 不得与调用方原 dict 共享引用；必须 deep copy 后深冻结。
- capability 绑定 actual payload bytes、backup_id、manifest digest、schema fingerprint、目标环境和 restore operation id。

**验收**：嵌套 dict/list、别名引用、`object.__setattr__`、序列化差异、并发修改测试全部在首次写入前 fail-closed。

### P0-03：恢复不是跨数据源原子操作，可返回部分成功

**证据**：writer 依次恢复 CRDB、cache SQLite、relay SQLite；CRDB 按表独立 BEGIN/COMMIT，SQLite helper 也按连接/表提交；异常被收集到 `result["errors"]` 后继续。前一数据源成功、后一数据源失败时无法整体回滚。

**风险**：覆盖恢复尤其危险——部分表已 TRUNCATE/DELETE 并提交，后续失败仍留下混合时代数据。调用方若只检查函数返回而未硬判 errors，会把半恢复当完成。

**根治逻辑**：

1. 不直接覆盖在线库；每个数据源恢复到新 staging DB/schema/bucket。
2. 对 staging 做行数、约束、FK、业务不变量、抽样 digest 和 schema fingerprint 校验。
3. 生成统一 restore plan 和 operation id；所有数据源达到 PREPARED 后才切换。
4. 切换使用可逆 alias/connection target；无法原子切换的资源用 saga + 明确补偿。
5. 任一表错误使整个 operation 状态 FAILED，禁止返回“恢复完成”。

**验收**：在每张表、每个数据源、切换前后故障注入；线上始终只看到旧全量或新全量，不出现混合状态。

### P0-04：migration manifest 与当前发布不一致，签名可信根未落地

**确定性问题**：

- manifest `release_commit` 仍为 `17241a8...`，不是 R63 `5d3f665...`。
- `tree_sha` 仍为 `6c9d989...`，不是 R63 `b165e11...`。
- 仓库已有 `004_effect_receipts_request_hash_unique.sql`，manifest 只列出 001–003。
- manifest 声称存在 `.sig/.pem`，递归树中未见这两个文件。
- `database/migrate.py::_load_migration_manifest()` 只解析 JSON；没有在应用迁移前执行 cosign 验签、identity/issuer 验证和 HEAD/Tree/完整 migration 集合绑定。
- 本次 Release 中 manifest sign/verify/binding 步骤实际 skipped。

**根治逻辑**：

- 构建阶段根据当前 HEAD/Tree 自动生成 canonical manifest，禁止手工旧 SHA。
- 自动枚举全部 migration，要求“磁盘集合 == manifest 集合”，不允许漏项或多项。
- 先产生 detached signature/certificate，再将签名证据作为不可变 release artifact；部署和迁移启动前都强制 cosign verify-blob。
- `migrate.py` 不能把未验签 JSON称作 signed trust anchor；验签失败、签名文件缺失、HEAD/Tree 不匹配必须阻断。
- 004 加入 manifest 并记录 before/after schema fingerprint。

### P0-05：高风险 outbox worker 会在未配置 provider 时静默“成功”

**证据**：`services.data_lifecycle.OutboxWorker` 明确是 stub；`provider_registry is None` 时，`run_once()` 不执行任何外部副作用，却调用 `complete_outbox_event(event_id)`。

**风险**：生产误启动默认配置后，Telegram/R2/CRDB/邮件/文件副作用全部被永久标记完成，业务状态与外部世界失配，且通常无法自动恢复。

**根治逻辑**：

```python
if provider_registry is None:
    raise RuntimeError("Outbox provider registry is required in non-test mode")
```

- 测试 stub 必须放到 test fixture，不进入生产类。
- 启动时校验每个枚举 effect type 恰有一个 provider；缺失即 readiness failure。
- provider 调用参数必须包含 `request_hash/idempotency_key`，当前接口只传 `payload_json` 不足以强制幂等。
- 完成事件时 CAS `WHERE status='in_flight' AND lease_owner=? AND request_hash=?`，并保存 external_id。
- 实现 lease 续约、stale 回收、DLQ 告警、人工重放、补偿状态机；当前注释明确 stale reconcile 尚未实现。

### P0-06：CLI 恢复路径与三段式备份发现模型不一致

`run_restore()` 先调用旧 `get_latest_backup()`，该函数枚举 `db_backup/db_backup_*.json` 单文件；随后又拒绝旧格式，并把这个旧 loader 的 `data` 传给严格三段式入口。它没有从 COMPLETE marker 出发发现并加载对应 manifest/payload，合法三段式备份也无法形成可靠 CLI 输入。

**修复**：CLI 只接受 `backup_id` 或 COMPLETE key；由 strict service 自行读取 COMPLETE→manifest→payload，调用方不得预加载/拼装 data。删除双重 loader 与嵌入 manifest 检测。

## 5. P1——上线前必须完成的正确性与安全整改

### P1-01：capability 的“私有”说明不成立，nonce 仅进程内

Python 下划线不是访问控制，外部代码仍可 import 模块属性。真正安全性应来自完整密码/状态验证，而非“外部无法访问 sentinel”的注释。`_CONSUMED_NONCES` 只是进程内 set；多实例、重启和 worker 切换后不保留。

**修复**：restore operation/nonce 在权威 SQLite/CRDB 表中原子消费；唯一键绑定 backup、target environment、manifest digest、requested tables、merge mode；重启与多实例测试必须拒绝重复 destructive restore。

### P1-02：outbox 冲突处理通过异常字符串猜测

UoW 把异常文本包含 `unique` 或 `constraint` 都当成“已排队”。这可能吞掉 CHECK、NOT NULL、FK 或其他约束错误，而不是仅处理目标唯一键冲突。

**修复**：使用数据库错误码/constraint name；冲突后 SELECT 既有行并逐字段验证 action/effect/target/request_hash/payload digest 一致，否则抛 `IDEMPOTENCY_CONFLICT`。

### P1-03：004 migration 的守恒断言主动降级

004 注释承认将严格等式简化为 `strict + quarantine <= original`，去重行未写入独立 evidence 表；这无法证明每条原始记录有去向。

**修复**：为 duplicate 建 `effect_receipts_r62_duplicates` 取证表，要求 `strict + quarantine + duplicates == original`；每个原始 row 记录 source rowid、classification 和 winner rowid。不得用 `GROUP BY` 静默丢失证据。

### P1-04：restore 遇到无合法列时可能遗留开放事务

CRDB restore 在 BEGIN 后，如果 `cols` 为空会 `continue`，该分支没有显式 COMMIT/ROLLBACK。应在开始事务前完成全部 schema/column 校验，并使用连接级 transaction context manager。

### P1-05：MFA 吊销仍依赖调用方“记得查询权威层”

同步 `verify_mfa_receipt()` 只查进程内 L1；注释要求跨进程调用方在验证前另行 await `is_mfa_receipt_revoked()`。安全正确性不应由每个调用点自行组合。

**修复**：提供唯一 async `verify_mfa_receipt_authoritative()`，内部完成签名、age、kid、SQLite/CRDB revocation、principal/purpose/action hash 和一次性消费；禁止生产代码调用低层 sync verifier。

### P1-06：按钮 token 的 action/audience 必须在 handler 显式匹配

handler 获取 `(valid, token_action, payload)` 后，审阅到的主要分支重点校验 payload/sub-action，但未形成统一的 `token_action == expected_action` 强制器。每个 report/restore/delete handler 必须拒绝跨 action handle，不能只依赖签名和 user id。

**修复**：`verify_button_token_by_handle(handle, actor, expected_action, expected_audience, expected_resource_version)`；库内部一次性完成全部绑定与 nonce 消费。

### P1-07：report 按钮结果仍含硬编码中文，说明 i18n 门禁是假闭环

`_handle_report_action` 仍直接拼接：`已提交封禁审批`、`已封禁用户`、`封禁失败` 等中文。当前 scanner/baseline 为绿却未阻断这些真实用户出口。

**修复**：所有结果使用 `UserMessage.from_key()`；中文和英文 locale 提供同构 ICU 参数；Telegram sink 类型只接受 `UserMessage`，渲染集中在 adapter 最后一层。

### P1-08：UserMessage 并未真正强制，且脱敏/不可变性是浅层的

- 文档写“所有出口只接受 UserMessage”，但示例仍 `reply_text(msg.render(...))`，最终 sink 接收裸字符串；没有类型级强制。
- frozen dataclass 内的 `params` 仍是可变 dict；只做浅拷贝，嵌套对象可被修改。
- 敏感值过滤依赖 key 子串和长度，敏感值放在 `value/data/code` 等普通 key 下仍可能泄漏。

**修复**：adapter 签名接收 `UserMessage|ErrorEnvelope`，禁止 str；params 使用 ErrorDefinition 的逐错误 public-field schema，递归冻结并按类型/长度验证；render 后立即发送，不在业务层传播字符串。

### P1-09：所谓 cross-function scanner 仍不是完整跨函数数据流分析

当前实现最大回溯深度 3，主要使用函数内赋值 map；对参数、全局、属性、下标和多数函数返回标记 unknown；部分位置参数 unknown_call 为避免误报并不失败。它不能证明 wrapper、alias、动态 dispatch 和第三方 adapter 全覆盖。

**修复**：使用 CodeQL/Semgrep taint mode 或 pyright/mypy 类型边界；把 scanner 降级为补充门禁，不再宣称完整 source-to-sink。

### P1-10：observability 债务从 278 增至 281

结构化 allowlist 改善了可追踪性，但 281 条统一写成 `R62 observability debt - pending refactor`、同一 owner/ticket/expiry，不是逐项风险说明；数量还比 R62 的 278 增加。

**修复**：正式上线前归零；至少按模块分 ticket/owner/root cause/计划，不允许重新生成 fingerprint 来“刷新”基线。行号变化导致 fingerprint 全变说明当前指纹不稳定，应使用 AST 结构/规则/函数名而非裸行号。

### P1-11：错误码启动校验仍对 locale 文件读取 best-effort

ErrorDefinition 的三个交互字段已显式化，是实质进步；但 locale 文件读取异常时启动校验只 warning。Release 镜像应在启动前对打包后的实际 locale 文件 fail-closed，而不能只依赖源码 CI。

### P1-12：i18n ICU 失败仍允许弱回退

ICU 解析失败回退到普通 format 可能把 plural/select 模板原样或错误渲染给用户。Release 模式应在 locale 加载阶段预编译全部 ICU message；任一语法或参数集合不对称直接阻断构建。

## 6. 国际化、统一错误码、按钮式流程与无障碍专项

### 6.1 中英语言包

**已完成**：`SUPPORTED_LOCALES={zh-CN,en-US}`；key 对称门禁；login en-US 独立渲染测试；安全 fallback。

**仍需**：

- 删除 callback 中全部硬编码中英文用户文案。
- 对 2275 个 key 做“值质量”检查，不只比较 key：禁止 en-US 中文副本、空值、参数差异和未翻译占位符。
- 预编译 ICU；校验 plural/select 分支、参数类型、HTML escaping、日期/时区/数字格式。
- locale cookie 设置 Secure/HttpOnly/SameSite/Max-Age/Path，并验证 open redirect 与 Referer 清洗。
- 语言切换必须覆盖 Web、所有 Bot、邮件、通知、SSE/WebSocket。

### 6.2 统一错误码

- 保留单一 `ErrorDefinition` registry。
- 增加 `transport_status/retry_policy/public_schema`，避免只靠 HTTP status/retryable。
- 强制 destructive/data-integrity/financial 错误 `show_retry_button=False`，除非有明确幂等证明。
- 前端与 Bot 不得展示 `result.error` 原始字符串；只按 code+locale 渲染。
- trace_id 可展示，exception/SQL/provider payload 不可展示。

### 6.3 按钮式流程

标准流程必须统一为：服务端 intent → 签名 handle → actor/action/audience/resource version/MFA 验证 → 双人审批 → UoW/outbox → 本地化结果与审计。

- ban、restore、maintenance、purge、永久删除必须 MFA+双人审批。
- detach/block/软删除是否免审批必须有风险分级与可撤销证明，不能只写 metadata。
- nonce、handle、approval、effect receipt、outbox 使用同一个 action_id/request_hash。
- 重复点击必须返回同一结果，不重复执行，也不能显示原始异常。

### 6.4 无障碍

**已完成**：依赖缺失 hard fail、生成用例数检查、zh/en 双 locale、axe/键盘/焦点/dialog/aria-live/zoom/reduced-motion。

**上线补充**：

- `--list | grep -c '.*'` 会把标题/空白等也计数，应解析 Playwright JSON reporter 并断言每个 route×locale 的精确 expected count。
- 禁止 test.skip/fixme/only；执行后解析 JSON，要求 skipped=0、expected cases 全部 completed。
- 增加真实屏幕阅读器语义快照、400% reflow、触控目标、超长英文/中文、网络错误和按钮 busy 状态。
- 动态参数路由必须使用真实 fixture，不得因无法构造参数而从 generated cases 排除。

## 7. 数据、备份、恢复与迁移

- 备份验证应始终从 COMPLETE marker 开始，而不是调用方提供 `data`。
- 备份与 manifest 使用签名 envelope；恢复 writer 只读取验证后的 immutable bytes。
- 三次全新环境恢复必须覆盖 CRDB、cache SQLite、relay SQLite、R2 引用和 Redis 可重建状态。
- 每次演练记录 RPO/RTO、行数、约束、业务不变量、抽样 digest、失败注入和回滚结果。
- 旧备份离线转换工具必须真正实现；当前 CLI 只是提示存在该工具，不能把文案当功能。

## 8. CRDB 空载 RU 最终方案

### 8.1 硬目标

- bot role：`0 RU/day`
- 系统理想空载：`≤20 RU/day`
- 正式上线最大空载：`≤100 RU/day`
- `>500 RU/day`：Release 自动失败
- 活跃成本：`≤250 RU/DAU/day`
- 月度预算：`≤35M RU`

### 8.2 实现逻辑

1. `CRDB_POOL_MIN_SIZE=0`、max=2；无业务请求不建立连接。
2. 停止 CRDB 心跳、轮询、leader election 和定时全表扫描；由 Redis/event/平台调度唤醒。
3. backup 使用 Cockroach Cloud managed backup；应用不得逐行扫描 CRDB 制造 RU。
4. 所有 query 按 fingerprint 归因；未知 RU 归因视为缺陷。
5. 缓存使用事件失效和负缓存，不做周期性刷新。
6. 批量写、UPSERT、最小列查询；热点 SQL 建 RU budget 和 EXPLAIN 证据。
7. outbox worker 无事件时指数退避并退出连接，不得 2 秒轮询 CRDB。

### 8.3 验收

生产等价配置零用户、零任务连续 72 小时；导出每小时 RU、连接数、query fingerprint、后台任务次数。没有这份证据不能以“配置看起来正确”判定上线。

## 9. SDLC 八阶段终审

1. **需求**：量化 SLO/SLA、RPO/RTO、RU、WCAG 2.2 AA、双语完整率、审批和供应链目标。
2. **架构**：恢复 staging/switch、强制 transactional outbox、唯一消息/错误协议、同 digest 发布。
3. **设计**：输出 restore plan、capability schema、outbox provider contract、manifest signing policy 和按钮状态机。
4. **实现**：关闭全部 P0/P1，删除 stub 成功路径、浅冻结、旧 loader、弱守恒和硬编码文案。
5. **验证**：单元、属性、并发、故障注入、迁移、双语、a11y、灾备、供应链与 RU 全覆盖。
6. **发布**：连续三次 Release success；required job 不得 skipped；只部署受证明 digest。
7. **运营**：SLO、DLQ、stale lease、restore、approval、MFA revocation、RU 和签名失败告警。
8. **退役**：旧备份转换/销毁、旧错误码兼容期、key rotation/revocation 和可证明数据删除。

## 10. 一次性整改顺序

### Wave 0：继续冻结生产发布

- 禁止手工绕过 Release；固定 R63 SHA、tree、image digest 和证据目录。

### Wave 1：关闭 P0

1. 修复 cosign identity，使 Release 全链继续执行。
2. 深冻结 restore payload，并对实际写入 bytes 重新 hash。
3. 恢复改 staging + validate + switch，禁止跨库部分提交。
4. 重建当前 HEAD/Tree/001–004 的 signed migration manifest，并在 runtime 强制验签。
5. 删除 outbox stub 成功行为，接入完整 provider、lease recovery、DLQ、saga。
6. CLI 改为从 COMPLETE/backup_id 开始严格发现。

### Wave 2：关闭 P1

- 权威 MFA/restore nonce、精确约束冲突、004 等式守恒、按钮 action binding、i18n 硬编码、UserMessage 类型边界、scanner、281 条 observability 债务全部关闭。

### Wave 3：环境证据

- 3 次空白恢复；7 天多实例 soak；72 小时空载 RU；双语+a11y 全路由；并发审批/按钮/outbox/restore 故障注入；同 digest 供应链复验。

## 11. Production GO 硬门禁

- [ ]  本报告 P0/P1 全部关闭，无 stub、TODO、baseline 或 skipped 代替实现。
- [ ]  Release 连续 3 次全绿；rc-continuity、sign-image、manifest sign/verify、attestation 均 success。
- [ ]  manifest 绑定当前 HEAD/Tree 且完整列出 001–004；runtime verify-blob fail-closed。
- [ ]  restore payload 深度不可变，writer 对实际 bytes 重算 digest。
- [ ]  跨 CRDB/SQLite/R2 恢复不存在部分可见状态。
- [ ]  outbox 无 provider 时启动失败，不能标记完成；stale lease/DLQ/saga 可演练。
- [ ]  所有高风险按钮绑定 actor/action/audience/resource/MFA/approval/request hash。
- [ ]  用户出口无硬编码文案，UserMessage/ErrorEnvelope 成为类型强制边界。
- [ ]  observability 债务从 281 归零。
- [ ]  a11y expected case 精确对账，skipped=0；zh-CN/en-US 真实渲染。
- [ ]  3 次空白恢复、7 天 soak、72 小时 RU 证据全部达标。

## 12. 最终结论

R63 不是“无改进”：公开 restore skip 已删除，按钮 baseline 清零，004 migration、MFA age/revocation、错误交互字段和 a11y hard-fail 都是真实进展。但本轮仍存在数个比表面门禁更深的架构缺陷：浅冻结 payload 可在验证后修改；恢复跨三个数据源可部分提交；migration trust anchor 与当前发布脱节；outbox stub 能静默吞掉真实副作用；Release 仍在同一 image verification 节点失败。

**因此正式裁定保持：DEVELOPMENT PASS / STAGING CONDITIONAL GO / PRODUCTION NO-GO。** 完成 Wave 1–3 并满足第 11 节全部硬门禁后，才可重新申请生产上线终审。