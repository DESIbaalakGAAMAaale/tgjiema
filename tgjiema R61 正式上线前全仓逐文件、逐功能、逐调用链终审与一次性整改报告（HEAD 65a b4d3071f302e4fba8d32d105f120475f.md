# tgjiema R61 正式上线前全仓逐文件、逐功能、逐调用链终审与一次性整改报告（HEAD 65a2fe3）

<aside>
⛔

**终审裁定：DEVELOPMENT PASS / STAGING CONDITIONAL GO / PRODUCTION NO-GO。** 当前代码质量较 R60 明显提升，但最新 Release Gates 仍为 failure，且审批事务边界、恢复信任令牌、迁移可信根等仍存在确定性上线阻断项。完成本报告全部 P0、P1，并补齐真实环境证据后，才可提交 Production GO 复核。

</aside>

## 1. 审查基线与方法

- 仓库：`maxiuquan/tgjiema`
- R61 HEAD：`65a2fe382749a9202c6c8eca40ff2ef5493ed0eb`
- Tree：`db55588873d39d10bab75c8ac56590c7bf5e4067`
- R60 基线：`a93a23f9ccdc6e421f2f4a8685cdd18edb568997`
- 变更范围：R60→R61 共 4 个提交；HEAD 合并提交签名 verified/valid。
- 审查方式：以 R60 全仓审查为基线，对新 Tree、完整 compare、关键安全调用链、数据库迁移、备份恢复、MFA、i18n、错误协议、按钮流程、无障碍、CI/Release 和上线证据重新交叉验证；不以 commit message 作为问题关闭依据。
- 证据边界：静态源码与公开 Actions 可判定代码/CI；CRDB RU、真实对象存储、空白 VPS 恢复、生产密钥和多实例并发必须以真实环境测试证明，不能用单元测试替代。

## 2. CI / Release 权威结果

| 流水线 | 结果 | 终审含义 |
| --- | --- | --- |
| CI `29589322310` | success | 开发门禁通过 |
| E2E Tests `29589322816` | success | 当前 E2E 集合通过 |
| Release Gates `29589321136` attempt 2 | failure | 生产发布阻断 |
| `sign-image` | failure | Cosign keyless 按 digest 签名失败 |
| 签名验证 / provenance / source signing | skipped | 供应链证据链未生成 |
| `publish-attestation` | skipped | 证明未发布 |
| `release-summary` | failure | 聚合门禁正确阻断失败链 |

**结论：** R60 中“聚合 job 必须识别下游失败”的代码问题已基本修正，但真实 Cosign 失败没有关闭；重跑仍失败，不能上线。

## 3. R60 问题关闭矩阵

| R60 项 | R61 状态 | 结论 |
| --- | --- | --- |
| 审批 UPDATE commit 后检查 rowcount | 已改善 | 已改为 rowcount 检查后 commit |
| 两张 MFA receipt 分散消费 | 部分关闭 | receipt 与 approval CAS 已同事务，但未覆盖业务动作/outbox |
| 恢复 token 缺失继续执行 | 表面关闭 | None/invalid 会 raise，但调用方可自行构造 `valid=True`，信任根仍可绕过 |
| 缺 ciphertext hash 继续 | 已关闭 | 当前路径中止恢复 |
| COMPLETE 未绑定 payload_key | 已关闭 | canonical JSON 已覆盖 payload_key |
| 迁移无 checksum/事务 | 部分关闭 | 新增 checksum 与事务，但历史 checksum 回填为 trust-on-first-use |
| MFA AMR/TTL/iat | 大部关闭 | 仍缺 kid/轮换协议，且冒用 `v4.public` 命名 |
| i18n logger 邻行误判 | 已关闭 | 改为 AST 父 Call 分类 |
| Error presentation 显式化 | 部分关闭 | 字段已增加，但空值仍按 severity/retryable 回退 |
| A11y 导航吞错 | 已关闭 | 已增加 status/final URL/login/landmark 断言 |
| Release/Cosign/attestation | 未关闭 | 最新 Release 仍失败 |

## 4. P0—必须在上线前全部关闭

### P0-01：审批、MFA、业务动作与 outbox 仍不在同一权威事务

`_verify_break_glass_two_person_approval()` 现在只原子消费 `mfa_receipts` 和 `command_approvals`，随后立即 commit。真正的删除/恢复/隔离等业务副作用及 outbox 不在该提交点内。若 commit 后业务动作失败，审批和 MFA 已永久消费；若业务动作成功但 outbox 失败，则审计/通知链断裂。

**具体实现逻辑：**

1. 将验证函数改为纯验证，不自行 commit；返回结构化 `ApprovalGrant`。
2. 新建 `execute_high_risk_command_uow(command, grant)`，由唯一 Unit of Work 持有连接。
3. 同一 `BEGIN IMMEDIATE`/CRDB serializable 事务内依次执行：权限再授权→receipt CAS→approval CAS→业务状态变更→effect receipt/idempotency key→outbox→audit record。
4. 所有 rowcount、状态机前置条件和唯一键验证均在 commit 前完成；任何失败统一 rollback。
5. 外部不可事务副作用只能由提交后的 outbox worker 执行，使用 effect receipt 保证 exactly-once observable effect。
6. 增加双并发、业务失败、outbox 失败、进程在 commit 前后崩溃的故障注入测试。

**验收：** 1000 轮双并发仅一次成功；任一失败不得半消费；DB 状态、outbox、audit 与 effect receipt 可证明一致。

### P0-02：`BEGIN IMMEDIATE` 任意失败被误判为“已有事务”，且后续仍 commit

当前代码捕获所有 BEGIN 异常后直接“复用现有事务”，没有验证 `db.in_transaction`，随后无条件 `commit()`。锁竞争、连接损坏、I/O 错误均可能被误当作嵌套事务；若确实复用上层事务，又会擅自提交调用方事务。

**具体实现逻辑：**

- 仅在 `db.in_transaction is True` 时允许 savepoint：`SAVEPOINT approval_consume`；成功 `RELEASE`，失败 `ROLLBACK TO`。
- 非事务状态下 BEGIN 失败必须抛统一错误，禁止继续。
- 用 `owns_transaction` 标记决定是否 commit；被调用方绝不能提交上层事务。
- 删除宽泛“except Exception→复用”分支，并为 locked、closed connection、disk I/O、nested transaction 分别测试。

### P0-03：恢复“信任令牌”可被普通调用方自行伪造

`restore_from_backup_data()` 只检查对象存在且 `valid=True`。CLI 路径直接构造 `BackupValidationResult(valid=True, backup_id=...)`，并未证明该对象来自 strict validator。任何仓库内调用方都可复制这一做法，绕过 COMPLETE、manifest、digest、签名和解密验证。

**具体实现逻辑：**

1. 删除公开的 `_r59_validation_token` 参数和可构造的布尔信任对象。
2. 将低层写入器设为模块私有；唯一公开入口为 `validate_and_restore_backup_strict()`。
3. strict orchestrator 内完成读取、验签、hash、解密、schema、staging integrity 后，直接调用私有写入器，不把“已验证”状态跨 API 传递。
4. 如必须传 capability，使用无法从外部构造的私有哨兵/闭包，并同时绑定 manifest digest、payload digest、backup_id、目标库和过期时间；不能只看 `valid`。
5. CLI、BackupEngine、DisasterRecovery、测试工具全部路由到同一 strict orchestrator。

**验收：** 全仓静态门禁禁止直接调用私有 restore writer；构造 `BackupValidationResult(valid=True)` 无法触发任何写入；缺任一签名/hash/key 字段均硬拒绝。

### P0-04：Release 供应链仍为真实失败

Cosign keyless 签名失败导致 verification、SLSA provenance、source signing、artifact upload 和 attestation 发布均未完成。

**具体实现逻辑：**

- 校验 workflow `permissions: id-token: write, packages: write, attestations: write, contents: read`。
- GHCR 登录需具备 push 签名对象所需权限；签名 identity 和 issuer 必须固定验证。
- 打印脱敏后的 digest/identity/issuer/registry 响应，区分 OIDC、权限、TUF、透明日志和 registry 错误。
- 签名、SBOM、扫描、provenance、attestation 必须绑定同一不可变 digest；部署只接受该 digest。
- 不允许 `continue-on-error`、失败后上传空证据或生产 push 跳过。

**验收：** 新提交连续三次 Release Gates success；所有 required jobs 均 success；Cosign verify、SLSA、SBOM、attestation 与部署清单指向同一 digest。

### P0-05：历史 migration checksum 回填采用 trust-on-first-use

若旧 `_migrations_applied` 行没有 sha256，当前启动时直接用“当前磁盘文件”回填并 commit。若历史 migration 已被修改，首次升级会把被篡改内容登记为可信基线，无法检测篡改。

**具体实现逻辑：**

- 发布随版本签名的 `migration-manifest.json`，包含版本、sha256、前驱版本和 release commit；CI 用签名/受保护 tag 校验。
- 旧记录缺 checksum 时只能与发布 manifest 比对，不得从工作树自举信任。
- checksum 回填、schema fingerprint 验证和版本记录必须处于明确升级事务；不匹配立即阻断启动并给出恢复 runbook。
- 禁止 `INSERT OR REPLACE` 改写已应用历史；使用一次性 `INSERT`，冲突即错误。

## 5. P1—商用上线质量门禁

### P1-01：003 migration 仍迁移伪造 request_hash

SQL 把空 hash 替换为 `r60_invalid_skip`，但注释又称只迁移合法行；新表未对 hash 长度/hex、permission 非空、expires_at ISO 时间、MFA receipt 非空字符串设置 CHECK。`NOT NULL` 不等于有效。

**修改：** 非法行只进入独立 quarantine 表；严格表仅迁移满足 `length(request_hash)=64`、小写 hex、`length(trim(permission))>0`、receipt 非空和可解析时间的记录。迁移前后记录数、合法数、隔离数必须守恒；禁止 `INSERT OR IGNORE` 静默丢行。

### P1-02：migration SQL 分割器与“可忽略错误”策略风险

逐分号切 SQL 容易破坏 trigger/字符串；`already exists`、`duplicate column` 白名单可能把 schema 漂移伪装成成功。

**修改：** 使用 SQLite 原生完整语句解析或每个 migration 为 Python callable；正式 migration 不允许忽略 DDL 错误。执行后用 `sqlite_master`、PRAGMA table_info/index_list/foreign_key_check 断言目标 fingerprint。

### P1-03：MFA receipt 协议仍无 kid/轮换且命名误导

当前是自定义 HMAC token，却使用 `v4.public` 前缀并称 PASETO-like；没有 `kid`，密钥轮换、双钥验证、撤销窗口和算法敏捷性不足。

**修改：** 要么采用经过审计的标准 PASETO/JWT 库并严格校验 alg/kid/typ/issuer/audience，要么改为内部版本名 `mfa1`；密钥环按 kid 查找、旧 key 只验证不签发、轮换期短于最大 TTL，未知 kid fail-closed。

### P1-04：统一错误协议仍保留 349 项债务

baseline 仍为 349：security 27、destructive 9、data-integrity 35、observability 278 左右。配置写着零容忍目标，但实际门禁仅阻止新增，不能称上线终版。

**修改：** security/destructive/data-integrity/financial 全部归零；observability 每条必须显式 allowlist（owner、原因、到期日），禁止空 allowlist 配合 305 总量上限。最终 baseline 只能记录 0，而非冻结技术债。

### P1-05：ErrorDefinition 的 presentation 仍非真正强制显式

字段默认空串/None，导出时仍按 severity/retryable 推断，与“每个错误显式定义交互”目标不符。

**修改：** 删除默认值和回退；构造 ErrorDefinition 时强制填写 `presentation/show_retry_button/audit_level`；启动校验枚举合法性、所有 code 唯一、message key 双语存在、safe_params 与 ICU 参数一致；未知 code 记录安全审计并统一映射 INTERNAL。

### P1-06：i18n 的“完整 CLDR”表述不成立

ordinal 仅实现 en/ru/zh，plural 仍是 zh/en 简化，其他 locale 默认英文/other；production 对 malformed ICU、缺参数和缺 key 采用安全文案回退，会掩盖发布缺陷。

**修改：** 当前若产品只支持 `zh-CN/en-US`，明确锁定 supported locales 并拒绝其他 locale；使用 Babel/ICU 标准库，不自研 CLDR。CI 对中英 key、参数、plural/select/selectordinal AST、HTML 安全上下文做双向对称检查。生产可向用户安全回退，但同时必须触发高优告警；release 构建对任何缺 key/缺参/malformed ICU 直接失败。

### P1-07：硬编码 scanner 仍只扫描已知 sink

AST 改造关闭了 logger 邻行误判，这是有效修复；但“其他调用非 sink 不收集”，新增 wrapper、模板 helper、WebSocket/SSE/JSON response 等用户出口可能漏扫。

**修改：** 建立 taint/source-to-sink 规则：所有字符串字面量默认可疑，仅协议常量显式 allowlist；扫描 render context、Response/JSONResponse、bot wrapper、HTML/JS DOM、邮件和通知。新增 sink 必须先登记；CI 输出文件/行/调用链，user-visible 必须为 0。

### P1-08：无障碍路由仍依赖人工 TEMPLATE_TO_ROUTE

现在 route inventory 会校验人工数组不漏项，优于 R60；但测试执行对象仍来自人工映射和 exclusions。`page.url().toContain(expected)` 也可能接受错误的相似路径；`allowRedirect` 未严格断言预期落点。

**修改：** 从应用 router 自动生成可访问测试 case，路由元数据声明模板、参数 fixture、权限和预期落点；用 URL parser 严格比较 pathname/query；locale redirect 明确断言 303、Set-Cookie、安全 Referer 校验和最终路径。每个中英页面执行 axe、键盘遍历、焦点可见、dialog trap/Escape/restore、aria-live、200% zoom 和 reduced-motion。

### P1-09：按钮式流程仍需全域状态机证明

按钮安全基础设施已存在，但需证明所有高风险 handler 都通过同一 ButtonFlow/CommandBus，而不是仅关键路径接入。

**修改：** 生成 handler inventory；高风险动作必须声明 action type、RBAC permission、MFA、双人审批、idempotency key、state transition、timeout、cancel、localized confirmation 和 audit schema。静态门禁禁止 handler 直接执行 destructive service；回调签名绑定 user/chat/action/payload hash/expiry/nonce。

### P1-10：CRDB 近零空载 RU 尚无真实证据

源码配置和单测不能证明免费层空载成本。当前没有 72 小时账单/metrics 证据，也没有 7 天 soak。

**修改：** 保持 bot role 空载不连 CRDB：pool min=0、无 keepalive、无轮询、无定时 SELECT、无空 outbox 扫描；事件唤醒+指数退避；管理/备份指标从 Cockroach Cloud API 或离线账单读取，不反向查询业务库。目标：bot 0 RU/day；总空载理想 ≤20、硬上限 ≤100；>500 阻断发布。

## 6. SDLC 八阶段裁定

1. **需求**：核心需求清楚；需冻结 supported locales、RPO/RTO、RU SLO、错误交互协议。
2. **架构**：事件驱动、outbox、按钮安全方向正确；高风险 Unit of Work 与 strict restore 单入口尚未完成。
3. **设计**：ErrorRegistry/i18n/a11y 已改善；默认回退和人工清单仍制造隐式行为。
4. **实现**：CI 通过，但存在 P0 事务/恢复 capability/迁移可信根问题。
5. **测试**：4067 测试声明与 CI success 是正面证据；仍缺真实并发崩溃、Cosign、空白恢复、RU soak。
6. **部署**：镜像 build-once 与 digest 验证通过；签名、provenance、attestation 未闭环。
7. **运维**：需要可执行告警、密钥轮换、迁移失败、restore、回滚和 RU runbook。
8. **退役**：需验证数据导出、密钥销毁、对象存储保留、用户删除和审计保留策略。

## 7. 一次性整改顺序

1. 先修 strict restore 单入口与不可伪造信任链。
2. 重构高风险 Unit of Work，统一 approval/MFA/business/outbox/audit。
3. 修复事务 ownership/savepoint。
4. 建立签名 migration manifest，重写 003 合法行/隔离/守恒逻辑。
5. 将错误协议债务归零并强制显式 presentation。
6. 锁定 zh-CN/en-US，改用标准 ICU/CLDR，扩展 sink 扫描。
7. 自动生成 Admin/A11y/Button handler inventory。
8. 修复 Cosign OIDC/registry 权限，闭合同 digest 供应链。
9. 执行真实环境证据矩阵：3 次 Release 全绿、72h 空载 RU、7d soak、3 次空白恢复。

## 8. Production GO 硬门禁

- [ ]  本报告全部 P0、P1 有源码、测试和 reviewer 证据。
- [ ]  Release Gates 连续三次 success，无 required job skipped。
- [ ]  OCI signature、SBOM、扫描、SLSA provenance、attestation、部署均绑定同一 digest。
- [ ]  高风险双并发、故障注入、崩溃恢复证明无半消费和重复副作用。
- [ ]  所有恢复入口缺任一信任字段均拒绝；普通调用方无法伪造 capability。
- [ ]  migration 历史签名可信、断电回滚、重复执行、非法旧行隔离及守恒全部通过。
- [ ]  中英 key/参数/ICU AST 对称；用户硬编码 0；错误协议债务 0。
- [ ]  Admin 全路由双 locale 的 HTTP、最终 URL、登录态、axe、键盘、焦点全通过。
- [ ]  72 小时空载 RU ≤100/day（目标 ≤20/day），7 天多实例 soak 无数据丢失/重复执行/权限绕过。
- [ ]  三次空白环境恢复达到既定 RPO/RTO，并通过真实业务探针。

<aside>
⚖️

**最终说明：** 本报告给出的是当前 HEAD 可确认的上线阻断项和商用整改蓝图，不对未来未知缺陷作“绝对无问题”保证。按上述门禁整改并取得真实环境证据后，项目才具备 Production GO 终审条件；在此之前不得将当前版本判定为可正式上线。

</aside>