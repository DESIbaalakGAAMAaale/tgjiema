# tgjiema R65 正式上线前全仓逐文件、逐功能、逐调用链终审与一次性整改报告（HEAD 20bf989）

<aside>
⛔

**R65 终审裁定：DEVELOPMENT CONDITIONAL PASS / STAGING CONDITIONAL GO / PRODUCTION NO-GO。** Commit message 中的“PRODUCTION GO”不是审查证据。权威 Release Gates 仍为 failure，且新引入的 restore 蓝绿编排主要是状态记录骨架，尚未执行真实 staging 写入、真实校验或接管现有恢复入口。

</aside>

## 1. 审查基线

- 仓库：[maxiuquan/tgjiema](https://github.com/maxiuquan/tgjiema)
- R65 HEAD：`20bf989a4b1017296d5237288290aefadce2205b`
- Tree：`f7c10d6c1d469ca5c0cdb33a21498c3ff018b11d`
- Parent / R64：`3ed31cabcf808cdbd1f0d0637d18986937d0c3b4`
- Commit 时间：2026-07-19 11:24:54（Asia/Shanghai）
- 签名：verified / valid
- 变更规模：24,865 additions、2,565 deletions、27,430 total
- 审查范围：完整源码树、R64→R65 变更、Release/CI/E2E/Deploy Actions、恢复、迁移、outbox、MFA、审批、高风险按钮、国际化、统一错误码、无障碍和 CRDB RU。

> 本报告以实际源码与 Actions 为准。提交说明、测试数量和新建类名只能证明实现意图；不能替代真实调用链、真实生产环境和成功发布证据。
> 

## 2. Actions 权威结论

| 工作流 | Run | 结果 | 判定 |
| --- | --- | --- | --- |
| Deploy Check | 29671725819 | Success | 基础配置通过 |
| E2E Tests | 29671725815 | Success | 现有 E2E 集合通过 |
| CI | 29671725813 | 审查时仍显示 in progress | 不能计为成功证据 |
| Release Gates | 29671725806 | **Failure** | 生产发布阻断 |

Release 精确失败链：

- `verify-branch-protection`：本轮已经 success，属于真实改善。
- `rc-continuity` job `88151892449`：`Verify RC continuity` 失败。
- `sign-image` job `88151967906`：`Verify image signature (output verification statement)` 再次失败。
- 镜像验证失败后，migration manifest 重生、release artifact manifest 生成/签名、release binding 等后续步骤 skipped。
- `publish-attestation` job `88151995856`：skipped。
- `release-summary` job `88152026883`：failure。
- `production-evidence` 虽运行成功，但只生成 **dry-run evidence index**，不能证明 7 天 soak、三次恢复、72h RU 或真实故障注入已执行。

**生产结论：**不得用 E2E success、分支保护 success 或 dry-run evidence 覆盖 Release failure。

## 3. R64 整改关闭矩阵

### 已真实改善

- `VerifiedBackupPayload` 已改为单一 `canonical_payload_bytes` 来源；payload/tables 从同一 bytes 解码。
- canonical JSON 已禁止 `default=str`，并使用 `allow_nan=False`。
- outbox 删除无 provider 静默 complete 的 test stub 路径；registry 加载异常改为 fail-closed。
- outbox 引入 immutable envelope、lease version、DLQ 审计等设计。
- HighRiskPolicy 已覆盖 delete、detach、block、ban、restore、purge、角色变更等高风险动作，策略值默认 MFA + 双人审批 + resource version。
- branch protection job 本轮通过。
- MFA sync verifier 门禁、sink boundary scanner、按钮 UX policy、a11y matrix 文件和生产证据编排框架已加入。

### 未关闭或仅建立框架

- Release 签名验证仍失败。
- restore orchestrator 未执行真实 staging 恢复/校验，且旧 `db_restore.run_restore()` 仍直接进入原有 writer。
- typed sink 边界仍有 **486 项 baseline 违规**。
- a11y scanner 默认不传 Playwright report，会明确“跳过执行对等检查”后返回成功。
- production-evidence 当前 Release 仅生成 dry-run 索引。
- observability allowlist 债务仍保留，并未整体清零。
- policy 表的存在不等于所有执行入口已实际调用统一 policy、MFA、审批和 resource-version CAS。

## 4. P0 上线阻断项

### P0-01：Release Gates 仍失败，供应链未闭环

**现状：**cosign sign 后的 verify 连续多轮失败。当前工作流使用：

`EXPECTED_IDENTITY=https://github.com/<repo>/.github/workflows/release-gates.yml@refs/heads/<branch>`

但未取得成功验证 statement；release manifest、provenance、attestation 和 publish 均未形成。

**一次性整改：**

1. 固定 cosign 版本并在最小独立 job 中打印 `cosign version`、证书 SAN、OIDC workflow ref、签名 bundle 和 verify stderr。
2. 使用 `cosign verify --certificate-identity-regexp` 时必须是严格转义、锚定仓库和工作流、仅允许预期 ref/commit；或按当前 cosign/Sigstore 实际 SAN 精确匹配。
3. 不使用不兼容的 `--output-verification` 契约；先验证当前版本帮助输出，再以 `--output json`/bundle 的受支持方式写 statement。
4. statement 必须验证 image digest、source commit、workflow identity、issuer、Rekor inclusion 和 certificate validity。
5. 所有后续 job 只消费该 statement 中的同一 digest；任何 skipped 即 release failure。

**验收：**连续三次 Release Gates success；sign、verify、SBOM、provenance、release manifest、attestation、publish/deploy 均绑定同一 digest，无 required job skipped。

### P0-02：RestoreOrchestrator 是控制面骨架，不是可用恢复引擎

**源码事实：**

- `provision_staging()` 对 SQLite 仅 `Path(...).touch()`；CRDB 只生成 staging 名称并记录状态。
- 注释明确“实际 provision 由 db_restore/cache_store/relay_db 完成”，但当前方法没有调用它们。
- `restore_to_staging()` 注释明确“实际写入由 db_restore 完成”，实际代码只把 datasource status 改为 `restored`。
- `validate_staging()` 对 schema、row count、FK、business invariant、hash、dry-run 六个维度，未调用真实验证器；只要 fault hook 不抛异常就直接写 `ok`。
- 现有 `db_restore.run_restore()` 未接入 orchestrator，仍通过 `validate_and_restore_backup_strict()` 调原 writer，并对 CRDB/两个 SQLite 分组恢复。

这是最严重的新问题：**状态机可以产生“已恢复、已验证”的审计记录，但没有发生真实恢复和验证。**

**一次性实现逻辑：**

1. `RestoreBackend` Protocol 必须提供：`provision() / load_verified_payload() / validate() / prepare_switch() / commit_switch() / rollback_switch() / destroy()`。
2. 分别实现 CRDB、cache SQLite、relay SQLite backend；禁止 orchestrator 自己伪造状态。
3. `restore_to_staging()` 必须接收 `VerifiedBackupPayload` 与 backend，真实写入 staging，返回 row counts、content hash 和 schema fingerprint；只有 backend receipt 验签/校验成功才能更新状态。
4. `validate_staging()` 必须逐维调用真实 validator；任何维度 `skipped/pending/unknown` 均失败，不能默认 `ok`。
5. 现有所有恢复入口统一路由到 orchestrator；CI 用 import/call-graph gate 禁止生产代码直接调用旧 writer。
6. 蓝绿切换必须为真实 active pointer/CAS：CRDB schema/database routing + SQLite 原子 rename/symlink/config generation；切换版本需持久化 fencing token。
7. 审批必须验证 approval 状态、requester/approver、权限、action hash；MFA 必须验证权威 receipt，而不是仅比较两个 ID 字符串。
8. operation 只能从数据库重新加载；进程内缓存不得作为权威状态。

**故障测试：**在三个 backend 的 provision/load/validate/prepare/commit 每个阶段注入 kill -9、超时、磁盘满、网络分区、CAS 冲突；active 数据始终保持单一时间点。

### P0-03：恢复审批与 MFA 当前只比较标识符

`execute_blue_green_switch()` 当前核心校验是传入 `approval_id == operation.approval_id`、`mfa_receipt_id == operation.mfa_receipt_id`。这不能证明：

- 审批已批准且未过期；
- requester 与 approver 不同；
- approver 具备正确角色；
- receipt 签名有效、未吊销、未消费、action hash 与本次 restore 匹配；
- backup/manifest/payload/switch version 未变化。

**整改：**切换方法只接受由 `ApprovalAuthority.verify_and_consume()` 与 `MFAAuthority.verify_and_consume()` 返回的不可伪造 capability；在同一 UoW 中 CAS 消费 approval、MFA、operation phase 和 switch fencing token。

### P0-04：生产证据 job 是 dry-run，且不阻断晋级

Release job 显示 `Generate dry-run production evidence index` 成功。dry-run 只能证明脚本可被调用，不能证明任何生产 SLO。

**整改：**

- PR/普通 push 可运行 schema/dry-run，但必须标记 `evidence_mode=dry_run`，禁止输出 production passed。
- production promotion 只接受独立、签名、不可变、未过期的真实证据 artifact。
- 每类证据含环境 ID、commit、image digest、起止时间、原始数据 digest、执行者、审批人和签名。
- `SOAK_7DAY`、`RESTORE_3X`、`OUTBOX_FAULT_INJECTION`、`RU_72H`、`SUPPLY_CHAIN` 任一缺失/过期即阻断。
- 禁止 `--skip` 用于 production promotion。

### P0-05：高风险策略尚未证明全调用链强制执行

HighRiskPolicy 的规则定义正确方向明显，但 helper 对未知 action 返回 False；若新 destructive action 未注册，可能被当作低风险。还需证明 CommandBus、callback、API、CLI、定时任务全部经过 policy。

**整改：**

- 对 destructive namespace 使用 fail-closed：未知 action 抛 `HIGH_RISK.ACTION.UNREGISTERED`，不能返回 False。
- 从路由、handler、CLI command 和 scheduler 自动枚举 destructive entrypoint，与 policy 集合做双向相等检查。
- policy enforcement 必须位于 CommandBus 执行器，不在 handler 可选调用。
- 每次执行保存 policy version、resource version、tenant、session、actor、request hash、approval/MFA receipt。

## 5. P1 上线前必须关闭

### P1-01：Typed sink 声称完成，但 baseline 仍有 486 项违规

baseline 明确记录：

- 总违规：486；
- `bots/admin_bot/handlers.py`：241；
- `bots/admin_bot/callback.py`：64；
- `bots/up_bot.py`：64；
- `bots/idx_bot.py`：46；
- 其余 conversation、dsp、mon、menus 等仍直接导入或调用 Telegram sink。

CI 运行的是 baseline ratchet，不是 `--strict`。因此“typed sink 已完成”不成立。

**整改：**迁移全部 486 项；CI/Release 只允许运行 `--strict`；adapter 输入必须是 `UserMessage | ErrorEnvelope | TypedMedia`，业务模块不得直接导入 Telegram/FastAPI response 类。

### P1-02：A11y 64 矩阵是清单，不是 64 个已执行测试

scanner 默认调用不带 `--test-report`；代码明确在无报告时返回 `ok=True` 并输出“跳过执行对等检查”。因此 CI 中 Scanner 9 只验证 generated JSON 的笛卡尔积，不验证 Playwright 实际执行数、skip 或失败。

**整改：**

1. Playwright 配置启用 JSON reporter，输出固定 artifact。
2. E2E 后强制运行 `check_a11y_matrix_enforcement.py --test-report <report>`。
3. 无报告、解析失败、executed ≠ 64、任何 skip/flaky/timedOut/unexpected 均失败。
4. 不得把 `failed/flaky/timedOut` 计作合格 executed；要求 64 个最终状态全部 passed。
5. generated case 必须真正映射到测试函数与 route/state fixture，禁止仅有 JSON 行。

### P1-03：国际化仍未形成严格出口边界

486 个 sink 旁路意味着裸字符串、混合语言和内部错误泄漏风险仍广泛存在。重点要求：

- zh-CN/en-US key 集合、ICU AST、参数名称和类型完全一致；
- en-US 禁止中文占位副本，zh-CN 禁止英文业务文案泄漏；
- locale 必须绑定 user/session，不得依赖全局默认；
- `query.message.text + localized_text` 等拼接禁止跨 locale 继承旧消息；
- 内部 exception 不得作为用户参数，只返回 safe message + error code + trace id；
- locale 文件读取或 ICU 解析失败时，staging/production 必须 fail-closed。

### P1-04：统一错误码仍保留 observability allowlist

security/destructive/data-integrity/financial 路径扩充并声明 0 是进步，但 baseline 中仍存在 admin/services 的历史 `except Exception: pass`、裸返回、裸 ValueError/RuntimeError 和裸字符串债务。allowlist 不是修复。

**整改：**上线前清空 allowlist；所有异常归一到 ErrorDefinition，定义 message_key、safe params、presentation、retryability、retry button、audit level 和中英翻译。

### P1-05：Outbox fencing 仍需验证真实存储语义

- reclaim 若把 `lease_version` 重置为 0，会重新使用旧 fencing 值 1，存在 ABA 风险；fencing token 必须单调递增，永不重置。
- provider “必须幂等”不能只靠接口文档；需外部 idempotency ledger/真实 provider 支持证明。
- `test_mode` 参数仍保留在构造器，即使文档声称忽略，应删除以减少误解和未来回归面。
- compensation 仍需证明由持久化 outbox 执行，而不是仅 Python callback。

**整改：**claim 使用 `lease_version = lease_version + 1`；reclaim 只清 owner/status，不重置版本；complete/fail/renew 全部 CAS 当前版本。

### P1-06：Canonical payload 仍需构造时强校验

当前 `payload` property 调用 `json.loads()`，但 `__post_init__` 主要计算 SHA。应在构造时一次性验证：bytes 类型、UTF-8、JSON object、无重复 key、schema、顶层 tables 类型、canonical round-trip bytes 完全相等。否则“任意 JSON bytes”可能被称为 canonical。

### P1-07：现有恢复 writer 仍可原地覆盖

即使 orchestrator 文件存在，旧 run_restore 仍接入旧 writer。必须删除或 capability-seal 所有 legacy direct restore，确保生产入口不能回退到原地覆盖。

### P1-08：Migration 005/006/007 需纳入签名 release manifest

本轮 Release 在生成 release manifest 前已经失败，因此新增迁移尚无已签名、同 digest 的生产证据。必须验证 001–007 全集合、顺序、hash、前驱、DDL version 和回滚策略。

### P1-09：CRDB RU 仍只有阈值脚本，没有 72h 数据

`POOL_MIN_SIZE=0` 和检查脚本不证明空载 RU。需要真实 72h 账单/metrics，并按 SQL fingerprint、service、job 归因。

目标继续为：bot role 0 RU/day；理想集群 ≤20 RU/day；硬上限 ≤100 RU/day；>500 RU/day 阻断；活跃期 ≤250 RU/DAU/day；月度 ≤35M RU。

### P1-10：CI 在审查截止时未形成 completed success

不得从 commit message 的“5580 passed”推导权威 CI 通过。R65 报告以 API 当前状态为准；Production GO 必须等待同 HEAD 的 completed/success，并保存 job 级结果。

### P1-11：按钮式流程要从 policy 表落到真实 UX

ButtonUXSpec 需要在真实 Telegram/Web handler 上验证：目标、影响、不可逆性、审批状态、MFA 状态、resource version、确认、取消、过期恢复、重试和返回按钮均真实渲染；不能只检查 sidecar metadata。

### P1-12：分支保护已通过，但需与 Required contexts 动态一致

本轮改善成立。仍需确保 ruleset context 名与实际 job name 双向一致、禁止 admin bypass、dismiss stale approvals、required conversation resolution、signed commit、独立 reviewer。

## 6. 国际化、统一错误码、按钮式流程专项结论

| 专项 | R65 状态 | 上线阻断 |
| --- | --- | --- |
| 中英语言包 | 框架增强 | 486 个 sink 旁路使统一 locale 边界未成立 |
| 统一错误码 | 高风险域改善 | observability allowlist 与内部错误参数仍需清零 |
| 按钮安全 | HighRiskPolicy 已建立 | 未知 action fail-open、全调用链 enforcement 未证明 |
| 按钮 UX | 规则/renderer 已加入 | 需真实 handler 双语言、过期与错误恢复 E2E |
| 无障碍 | 64 case 清单已建立 | Scanner 默认跳过实际执行对等 |

## 7. SDLC 八阶段

1. **需求：条件通过**
    - SLO、RTO/RPO、RU、语言和高风险矩阵已更明确；需变成不可跳过的验收契约。
2. **架构：不通过生产门槛**
    - restore 控制面与真实数据面未集成；旧 writer 仍是生产路径。
3. **设计：条件通过**
    - canonical bytes、HighRiskPolicy、outbox fencing、typed adapters 方向正确；多处仍是框架或 baseline。
4. **实现：不通过生产门槛**
    - restore 的 provision/restore/validate 可在不操作数据时产生成功状态。
5. **验证：条件通过**
    - E2E success，但 a11y 64 矩阵未证明真实执行；CI 尚未完成；真实证据为 dry-run。
6. **发布：不通过**
    - Release failure、sign verify failure、attestation skipped。
7. **运维：不通过生产门槛**
    - 无 72h RU、7d soak、3x blank restore、真实 outbox chaos。
8. **持续改进/退役：条件通过**
    - 必须删除旧 restore 与全部 sink 旁路，而不是长期 baseline 保留。

## 8. 一次性整改顺序

### Wave 0：修复发布链

- 修复 cosign verify；完成同 digest release chain。
- 连续三次 Release success，RC continuity 自动转绿。

### Wave 1：实现真正的恢复数据面

- 实现三个 RestoreBackend；真实 staging 写入与验证。
- 统一所有入口；审批/MFA capability 化；真实 CAS 蓝绿切换。

### Wave 2：关闭旁路

- 486 个 sink 违规降为 0并启用 strict。
- legacy restore/outbox/MFA 旁路全部删除或构建期阻断。
- HighRiskPolicy 对未知 destructive action fail-closed。

### Wave 3：国际化、错误码、按钮和 a11y

- 双语言 ICU/schema/真实翻译全检。
- 清空 observability allowlist。
- 64 个 a11y case 全部真实 passed，无 skip/flaky。
- 所有高风险按钮真实双语言、键盘/屏幕阅读与错误恢复 E2E。

### Wave 4：生产证据

- 7 天多实例 soak；三次空白恢复；真实 provider chaos；72h RU；同 digest supply chain。
- 所有证据签名、绑定 HEAD/image/environment，production promotion 不允许 dry-run/skip。

## 9. Production GO 门禁

- [ ]  Release Gates 连续三次 success，无 required job skipped。
- [ ]  当前 HEAD 的 CI completed/success。
- [ ]  restore 三个 backend 真实执行，旧 writer 无法从生产入口调用。
- [ ]  三次全新环境恢复和全阶段故障注入通过。
- [ ]  approval/MFA/resource version 在同一 UoW 权威消费。
- [ ]  sink boundary strict=0，当前 486 项全部清除。
- [ ]  a11y 实际 64/64 passed，skip/flaky/timedOut=0。
- [ ]  中英语言包与统一错误码严格检查全部通过。
- [ ]  outbox fencing 单调、真实 providers 幂等、DLQ/replay/compensation 通过。
- [ ]  7d soak、72h RU 和 supply-chain 真实证据全部有效。
- [ ]  CRDB 空载 RU 达到目标并可按 SQL/service/job 归因。

<aside>
📌

**R65 结论：**R64 整改带来了大量有价值的安全结构，但当前不能上线。最优先问题不是继续增加类、脚本或测试清单，而是把新框架接入真实生产调用链：让 restore 真正写 staging、真正验证、真正切换；让 a11y 真正执行 64 个用例；让 typed sink 的 486 个旁路归零；让生产证据从 dry-run 变成真实签名证据；并使 Release Gates 连续全绿。

</aside>