# tgjiema R67 正式上线前全仓终审与一次性整改建议（HEAD 0375b6a）

<aside>
⛔

**R67 终审裁定：DEVELOPMENT PASS / STAGING CONDITIONAL GO / PRODUCTION NO-GO。** R66 的 manifest 自引用、build 后重生 manifest、tag identity、restore Optional 降级等关键代码问题已出现实质修复；但当前仍有 7 项生产阻断：HEAD 及本轮多次提交未签名、Release Gates 依赖第 4 次 attempt 才成功、Deploy Check 明确吞掉 `docker compose config` 失败、跨 workflow 的祖先成功回退不是同 SHA 验证、生产证据缺失仍显示 success、legacy restore 仍随生产镜像交付且可被环境变量解封、attestation 对 migration digest 缺失采用“passed=True warning”而不会进入 warnings。

</aside>

## 1. 审查基线

- 仓库：[maxiuquan/tgjiema](https://github.com/maxiuquan/tgjiema)
- R67 HEAD：`0375b6aa27a5f4dc103dbfc9c573031ad5b056cb`
- Tree：`1bb7a382ee3bc0af59efa024a270ba49b8058a5d`
- R66 基线：`93a24970cdf39a81bb0f8f938d82ddbbce9c9acb`。[[1]](https://app.notion.com/p/tgjiema-R66-HEAD-93a2497-f67ad35722434fa5b7f6dba194fc4793?pvs=21)
- 最新提交时间：2026-07-20 21:04:56（Asia/Shanghai）
- HEAD Git verification：`verified=false / reason=unsigned`
- 最新 Release Gates：[run 29744930688](https://github.com/maxiuquan/tgjiema/actions/runs/29744930688)，最终 success，但为 **attempt 4**。
- 同 SHA CI、E2E Tests 为 success；Deploy Check 因 `.github/workflows/**` 变更应触发，但其内部仍存在 fail-open 命令。
- 审查方法：固定 SHA 读取当前源码、R66→R67 提交链、Actions run/job、migration catalog、release/attestation、restore、部署和生产证据调用链；以代码和 job 结果为准，不采信 commit message 的“已关闭”声明。
- 验证边界：未取得真实生产 CRDB/R2/Redis/Telegram/VPS、账单和 production environment artifact，因此真实运行门禁不能由静态代码替代。

## 2. R66 整改复核

### 已真实关闭或显著改善

1. **Manifest 自引用已修正**
    - migration manifest 升级为 catalog v3，不再写 `release_commit/tree_sha`。
    - commit/tree 绑定迁移到未提交的 release manifest，设计方向正确。
2. **Build 前冻结 catalog 已实现**
    - catalog 在 docker build 前生成。
    - sign-image 从镜像读取 catalog 计算 digest，与 build 输出和 runner source 双重比较。
3. **Tag identity 拼接已修正**
    - cosign identity 使用完整 `${{ github.ref }}`，可区分 `refs/heads/...` 与 `refs/tags/...`。
4. **Restore Optional 降级已删除**
    - store/backends/approval/MFA 成为必需依赖，缺失即构造失败。
    - startup readiness 检查三个 backend、nonce ledger 和 rollback target 表。
5. **Legacy scanner 改进**
    - 整文件白名单缩小为函数/行范围白名单。
    - AST 解析失败改为 fail；增加 wrapper re-export 检测。
6. **Attestation bundle 已接入**
    - SLSA v1 `gitCommit`、bundle、Rekor 和 certificate validity 支持较 R66 完整。

### 只部分关闭

- publish-attestation 从“无 run 直接 SKIP”改为轮询，但又新增“祖先成功 run 可替代当前 SHA”的回退。
- legacy restore 没有从生产镜像移除，只增加环境变量 seal。
- 生产证据仍未执行；普通 push 的 RU gate 没有 artifact 仍 success。
- tag identity 代码已修，但没有真实 tag run 证明整条 tag/promotion 链成功。
- 供应链 semantic verifier 对 migration digest 缺失仍软放行。

## 3. Actions 权威结论

### 当前 SHA

| 工作流 | 结果 | 证据解释 |
| --- | --- | --- |
| CI | success | 当前测试与静态门禁集合通过 |
| E2E Tests | success | 当前 E2E 集合通过 |
| Deploy Check | success | **不可信为强门禁**：内部有 `docker compose config ... |
| Release Gates | success（attempt 4） | 之前 attempts 失败；尚不满足连续初次成功/不可变候选复验 |

### Release Gates 关键事实

- Run 创建于 21:08，最终第 4 次 attempt 才于 21:33 success。
- 前一 SHA `8d7d8e9` 的 Release Gates 为 failure。
- `crdb-ru-72h-attribution-gate`：下载步骤 success，但结果上传 skipped，说明没有真实 RU 文件；job 仍 success。
- 普通 master push 的 production promotion 不能证明 tag/environment 晋级。

**结论：**当前绿色证明“经过人工 rerun 后，当前代码门禁可完成”，不证明候选稳定、生产证据齐备或正式发布链已演练。

## 4. P0 生产阻断项

### P0-01 · HEAD 与本轮关键提交未签名，发布治理发生回退

当前 HEAD `0375b6a`、`8d7d8e9`、`985b0a1`、`090f26d` 等提交均显示 `verified=false / unsigned`。此前报告和 expected branch protection 将 signed commits 作为发布条件；现在未签名提交可直接位于 master。

**风险：**

- 无法证明提交者身份和提交内容的签名完整性。
- 表明 signed-commit ruleset 未启用、未覆盖 master，或存在可绕过的 admin/direct-push 路径。
- CI 对镜像签名不能弥补 source commit 本身未签名；两者是不同信任层。

**一次性整改：**

1. 为 master/main 和 `v*` tag 建立 active ruleset：require signed commits、禁止 force push/delete、禁止 admin bypass。
2. 禁止直接 push；只允许 PR + 两名独立 reviewer + stale approval dismissal + conversation resolution。
3. 重新以签名提交承载 R67 修复；不要仅在后续 commit 补签，因为历史 unsigned commit 仍在发布祖先链。
4. Release Gates 增加 `git verify-commit $GITHUB_SHA` 与祖先链签名策略；tag 使用 annotated signed tag 并验证 tag object。

**验收：**固定 release candidate 的 commit 与 tag 均 verified；通过 API/审计证明无 bypass actor。

### P0-02 · Deploy Check 吞掉 Compose 配置失败

`.github/workflows/deploy-check.yml` 包含：

`docker compose config --quiet 2>/dev/null || true`

这会把语法错误、变量缺失、无效引用、profile/secret/config 错误全部转为成功。后面的 Python 只做部分 YAML 字段检查，不能替代 Compose 引擎验证。

**风险：**Deploy Check 绿色时，生产 Compose 仍可能无法解析或启动。

**整改：**

- 删除 `2>/dev/null || true`，保留完整 stderr。
- 使用与生产相同 Compose 版本和环境占位文件执行 `docker compose config --quiet`。
- 对所有 profiles 执行 config；校验 secrets/configs/healthcheck/depends_on/network/volume/user/read_only/cap_drop。
- 生成 canonical rendered compose artifact 并绑定 SHA。

### P0-03 · publish-attestation 的“祖先成功回退”不是同 SHA 验证

当前逻辑在当前 SHA 没有 Deploy Check run 时，60 秒后接受 master 上最近 successful run，只要其 SHA 是当前 SHA 的祖先。

**为什么不成立：**“祖先”只说明历史包含于当前提交，不说明祖先之后没有影响部署的变化。Deploy Check 的 path 列表只有：

- `deploy_vps_per_bot.sh`
- `docker-compose.yml`
- `config/services.yaml`
- `.github/workflows/**`

但 Dockerfile、requirements、`.env.*.example`、migration/bootstrap、run_all、services、restore、security scripts、systemd 生成逻辑等也会影响部署。只要它们未在 paths 中，当前 SHA 没 run，就可能借用旧成功结果。

**整改优先方案：**required deployment workflow 不使用 paths 过滤，每个 release candidate 同 SHA 必跑。

**可接受替代方案：**使用 GitHub compare 精确计算 ancestor→current 全部变更，并由版本化、测试覆盖的 deployment-impact policy 判定无影响；任何 API 错误、merge commit、浅克隆、未知路径均 fail。单纯 `merge-base --is-ancestor` 不足。

### P0-04 · Release Gates 依赖 4 次 attempt 才成功，候选稳定性未达标

最新 run 的 `run_attempt=4`。这不是“连续三次 Release Gates success”，而是同一 run 前三次存在失败后才通过。最新修复对 GHCR pull 增加 63 秒退避有合理性，但当前证据仍显示发布链存在明显非确定性。

**整改：**

- 固定一个 commit/tree/image digest，不重新 build，连续执行三次 verify-only gate。
- build 只执行一次；后续三次只 pull 同 digest、验证签名/SBOM/provenance/catalog、执行 smoke/restore contract。
- 区分 registry eventual consistency 与真正 build failure；记录每次 pull HTTP/error 分类和传播时长 SLI。

**验收：**新候选首次 run success；同一 image digest 的三次独立 verify-only run 均 success，无人工 rerun。

### P0-05 · 生产证据缺失仍显示 success

当前 `crdb-ru-72h-attribution-gate` 没有 `ru_72h_data`，结果上传步骤 skipped，但 job conclusion 为 success。这仍延续 R66 的“无数据=绿灯”语义问题。

**整改：**

- 普通 push 改名为 `ru-evidence-contract-check`，缺数据输出 `not_applicable`，不得作为 production passed。
- tag/environment promotion 必须单独运行 `production-ru-72h-gate`；缺 artifact、下载失败、签名无效、时间窗不足、环境/digest 不匹配一律 failure。
- 对 7d soak、3x blank restore、provider chaos、RU 72h、supply chain 使用同样模型。

### P0-06 · Legacy 原地恢复 writer 仍随生产镜像交付，环境变量可解封

scanner 已改进，但源码仍保留 `run_restore()`、`validate_and_restore_backup_strict()` 与私有原地 writer。Dockerfile 使用 `COPY . .`，因此这些实现进入生产镜像。`ALLOW_LEGACY_RESTORE=1/true/yes` 即可越过入口 seal；检查没有同时要求 tests/scripts 环境或拒绝 `APP_ENV=production`。

**风险：**误配置、被攻破的进程或注入环境变量可重新启用生产原地覆盖路径。

**一次性整改：**

1. 生产镜像物理删除 legacy CLI/writer；仅测试镜像包含 compatibility 包。
2. 或让 writer 接受由 orchestrator 私有模块签发且绑定 operation/backend/staging target 的不可伪造 capability，绝不接受普通 env flag。
3. `APP_ENV=production|staging` 下检测到 `ALLOW_LEGACY_RESTORE` 立即启动失败。
4. CI 对最终镜像执行 import/symbol scan：legacy public entrypoint 必须不存在。

### P0-07 · Attestation migration digest 缺失被伪装成“通过且无 warning”

`_check_predicate_materials_migration()` 在标准 SLSA provenance 不含 migration digest 时返回：

- `passed=True`
- `severity="warning"`

汇总器先执行 `if c["passed"]: continue`，因此该项不会进入 warnings；strict 模式也不会升级为 error。这解释了提交说明中的“0 warnings”，但事实是 migration digest 未在 attestation materials 中直接绑定。

**风险：**报告把“未验证但由 source commit 间接推导”显示成 PASS，而不是可见的降级证明。

**整改：**

- 若采用间接绑定，必须形成完整可验证链：attestation source commit → trusted checkout tree → catalog file digest → release manifest digest → image catalog digest。
- 将该检查返回 `passed=False, severity=warning`；strict production 模式升级为 error，除非上述链全部独立验证成功。
- 更优方案：生成自定义 in-toto predicate，直接列出 migration catalog、SBOM、config、release manifest digest。

## 5. P1 上线前必须关闭

### P1-01 · Deploy Check paths 集合严重不完整

至少补充 Dockerfile、requirements/lock、`.env*.example`、deploy scripts、migration/bootstrap、run_all、config、services、systemd/templates、security/restore/release scripts。对于 required check，建议彻底移除 paths。

### P1-02 · Commit 签名检查与镜像签名检查应分离

source commit、annotated tag、OCI image、release manifest、attestation 五个对象必须分别验证，不得用 cosign image success 推导 Git commit 可信。

### P1-03 · 真实 tag 路径仍未有成功证据

代码已改用 `${{ github.ref }}`，但当前证据仍是 master push。必须创建受保护 RC tag 演练 tag identity、environment approval、production evidence、promotion 和 rollback。

### P1-04 · Release Gate 运行时间和重试需设预算

当前 run 经多次 attempt 总体跨越约 25 分钟。定义 registry propagation SLO、job timeout、重试上限和失败分类；超出预算不得无限 rerun。

### P1-05 · Restore readiness 只检查 SQLite 表，未证明 CRDB 权威状态

`check_startup_readiness()` 查询本地 `sqlite_master` 的 rollback target 表。若生产 active pointer/fencing 权威在 CRDB或外部 routing store，需要分别检查连接、schema、权限、CAS 和版本一致性，不能只看 SQLite 表存在。

### P1-06 · Restore 外部副作用仍需 recovery reconciler

SQLite rename、CRDB routing switch 与数据库 UoW不是同一原子事务。必须持久化 prepare intent、fencing token、backend receipts，进程重启后由 reconciler 完成或回滚。

### P1-07 · 精确白名单依赖固定行号，维护脆弱

scanner 用 `line_start/line_end` 白名单。普通注释或格式化会移动行号，导致误报；更危险的是大范围行区间可能意外包含新调用。改为函数 qualified name + AST signature + source digest，禁止行范围授权。

### P1-08 · Legacy tests/scripts 目录被整体跳过

tests 跳过合理，但 scripts 中可能包含真实运维入口。应区分离线 recovery tool 与测试脚本；生产运维脚本同样需 capability/approval/MFA 审查。

### P1-09 · Compose 校验还需运行态 smoke

在 rendered compose 上启动最小 profile，检查非 root、read-only filesystem、healthcheck、secrets mount、网络隔离、restart policy、graceful shutdown 和 migration ordering。

### P1-10 · 139 个 skipped 仍需正式 inventory

本轮提交声明 `6387 passed, 139 skipped`。生成 skip 清单：测试名、原因、owner、到期日、对应生产风险。Telegram、R2、CRDB、restore、security、deployment 相关 skip 在上线前归零。

### P1-11 · RU/soak/restore artifact 必须防重放

每份证据加入 nonce、environment ID、commit/tree/image/attestation digest、时间窗、执行器和审批者；promotion 消费后标记 consumed，禁止跨候选复用。

### P1-12 · 供应链检查名称与真实语义需一致

当前名为 `predicate_materials_source_tree_sha` 的检查实际验证 source commit。重命名并单独验证 `git rev-parse <commit>^{tree}` 等于 release manifest tree，避免审计误解。

### P1-13 · GHCR pull retry 应限定错误类型

当前所有 `docker pull` 失败都重试。仅对 404/manifest unknown/429/5xx 等瞬态错误重试；401/403、digest mismatch、TLS/签名错误应立即 fail，避免掩盖权限和安全配置错误。

### P1-14 · 普通 master push 不应生产正式候选

仅 release candidate/tag 流程推送并签名可晋级镜像；普通 push 使用临时 namespace和短 retention，避免每次修复 commit 产生被误用的签名镜像。

## 6. 数据、并发与安全专项复核

### 备份/恢复

- backend/authority 强依赖已改善。
- 仍需证明三个 backend 同一 backup point、真实 staging、schema/row/hash/invariant 全验证。
- legacy writer 物理存在仍是主要旁路。

### Outbox/外部副作用

- 继续要求 lease version 单调、CAS owner/version/request_hash。
- Telegram/R2/CRDB provider 的 crash-after-send-before-ack、worker takeover、DLQ replay 需真实证据。

### MFA/双人审批

- authority 注入已强制。
- 需验证 action hash 同时绑定 backup ID、manifest/payload digest、staging targets、switch version和resource version，而非只绑定部分字段。

### 供应链

- catalog v3 和 image catalog digest 是实质进步。
- 当前最弱点转为 unsigned source commits、祖先 workflow 结果复用和 attestation 降级检查不可见。

### 部署

- Deploy Check 的 Compose fail-open 是直接阻断。
- paths 不完整使历史成功结果复用风险扩大。

## 7. SDLC 八阶段结论

1. **需求：条件通过** — 发布真值和 production evidence 语义更清晰，但仍混用 success/not-applicable。
2. **架构：条件通过** — catalog/build/restore 方向正确；legacy path 与跨 workflow 祖先复用仍破坏单一可信路径。
3. **设计：条件通过** — fail-closed 大幅增强；部署影响判定和 attestation 降级设计仍不足。
4. **实现：不通过生产门槛** — Compose 错误被吞、legacy env bypass、warning 汇总逻辑错误。
5. **验证：条件通过** — CI/E2E 绿；Release 需 attempt 4，真实 production artifacts 缺失。
6. **发布：不通过** — HEAD unsigned、无成功 RC tag/promotion 证据。
7. **运维：不通过生产门槛** — 无 72h RU、7d soak、三次真实恢复和 provider chaos。
8. **持续改进：条件通过** — 修复速度快，但需停止用更多注释/测试声明替代不可变运行证据。

## 8. 一次性整改顺序

### Wave 0 — 立即修复治理与假绿

- 重新建立 signed commit/tag ruleset，禁止 bypass/direct push。
- 删除 Compose `|| true`。
- 删除祖先 run 回退，或实现严格 diff-equivalence policy。
- 修复 attestation warning 汇总。

### Wave 1 — 封死恢复旁路

- legacy writer 不进入生产镜像。
- 移除 `ALLOW_LEGACY_RESTORE` 生产解封机制。
- 用 orchestrator capability 绑定 staging target 和 operation。

### Wave 2 — 固定不可变候选

- 只 build 一次，固定 commit/tree/image/catalog/release-manifest/attestation digest。
- 对同一 digest 连续三次 verify-only success，无 rerun。

### Wave 3 — 真实环境证据

- 三次 blank restore。
- 全阶段 kill -9/网络分区/磁盘满/CAS 冲突。
- 7d soak、72h RU、provider idempotency/chaos。
- 原始证据签名、防重放、绑定环境与候选。

### Wave 4 — RC tag 正式演练

- signed annotated RC tag。
- tag workflow、environment approval、production evidence、digest-pinned deploy、rollback 全通过。

## 9. Production GO 硬门槛

- [ ]  Release commit 与 annotated tag 均 verified，master/tag ruleset 无 bypass。
- [ ]  `docker compose config` 任何错误立即阻断。
- [ ]  required workflows 全部验证同一 SHA，不复用祖先 success。
- [ ]  同一不可变候选首次通过，三次 verify-only success，无人工 rerun。
- [ ]  migration catalog digest 在镜像、release manifest、自定义 attestation 中直接或完整链式验证。
- [ ]  production attestation 不存在隐藏 soft-pass/warning。
- [ ]  legacy restore writer 不在生产镜像中，环境变量不能解封。
- [ ]  三个 restore backend 的真实 staging/validate/switch/rollback 通过。
- [ ]  三次 blank restore 和全阶段故障注入通过。
- [ ]  72h RU、7d soak、provider chaos 证据存在、签名、未过期、防重放。
- [ ]  139 skipped 完成 inventory，关键主链 skip=0。
- [ ]  受保护 RC tag 的完整 promotion/deploy/rollback 演练成功。

<aside>
📌

**R67 最终结论：**R66 报告识别的核心结构问题多数已被认真修复，尤其是 catalog 自引用、build 前冻结、tag identity 与 restore 必需依赖。但本轮新的深审证明，仓库尚不能正式上线：当前 source commit 未签名，部署门禁显式吞错，跨 workflow 用祖先成功替代同 SHA，生产证据依旧缺失，legacy restore 仍可由环境变量解封，attestation 的降级验证被错误显示为无 warning。完成本报告全部 P0、关键 P1、真实不可变候选复验与 RC tag 生产演练后，才具备 Production GO 条件。

</aside>